#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
Процедура ЗаписатьИсполнениеАкции(Документ, Акция, Действие, КлиентаПривел = Ложь) Экспорт
	УстановитьПривилегированныйРежим(Истина);

	НаборЗаписей= РегистрыСведений.ИсполнениеАкций.СоздатьНаборЗаписей();
	Запись= НаборЗаписей.Добавить();
	Запись.Дата= ТекущаяДата();
	Запись.Документ= Документ;
	Запись.ВидАкции= Акция.ВидАкции;
	Запись.Алгоритм = Акция.Алгоритм;
    Запись.Клиент = Документ.Партнер;
    Запись.Документ = Документ;
    Запись.Действие = Действие;
	
	НаборЗаписей.Записать(Ложь);
	
	УстановитьПривилегированныйРежим(Ложь);
КонецПроцедуры
#КонецЕсли
