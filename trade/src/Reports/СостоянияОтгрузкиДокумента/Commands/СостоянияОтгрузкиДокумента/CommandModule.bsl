&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	//ПараметрыФормы = Новый Структура("Отбор, ФиксированныеНастройки, КлючНазначенияИспользования, КлючВарианта, СформироватьПриОткрытии, ВидимостьКомандВариантовОтчетов, Документ");
	//ПараметрыФормы.СформироватьПриОткрытии = Истина;
	//ПараметрыФормы.ВидимостьКомандВариантовОтчетов = Ложь;
		
	Если ТипЗнч(ПараметрКоманды) = Тип("Массив") Тогда
		ЭтоМассив = Истина;
		Если ПараметрКоманды.Количество() > 0 Тогда
			ПервыйЭлемент = ПараметрКоманды[0];
		Иначе
			ПервыйЭлемент = Неопределено;
		КонецЕсли;
	Иначе
		ЭтоМассив = Ложь;
		ПервыйЭлемент = ПараметрКоманды;
	КонецЕсли;
	
	//ПараметрыФормы.Документ=  ПервыйЭлемент;
	//ПараметрыФормы.Отбор= Новый Структура("Документ", ПервыйЭлемент);
	////СформироватьПараметрыФормыНаСервере(ПараметрКоманды, ПараметрыФормы, ПервыйЭлемент, ЭтоМассив);
	//
	//ОткрытьФорму("Отчет.СостоянияОтгрузкиДокумента.Форма",
	//ПараметрыФормы,,
	//"Документ=" + ПараметрКоманды,
	//ПараметрыВыполненияКоманды.Окно);
	
	
	
		ПараметрыФормы = Новый Структура("КлючВарианта, Отбор, СформироватьПриОткрытии, ВидимостьКомандВариантовОтчетов", 
		Истина,
		Новый Структура("Документ", ПервыйЭлемент), 
		Истина, 
		Ложь);
	
	ОткрытьФорму("Отчет.СостоянияОтгрузкиДокумента.Форма",
			ПараметрыФормы, ,
			Истина,
			ПараметрыВыполненияКоманды.Окно);

КонецПроцедуры
