#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Параметры.Свойство("Подразделение") Тогда
		Подразделение = Параметры.Подразделение;
	КонецЕсли;
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура Отмена(Команда)
	Закрыть();
КонецПроцедуры

&НаКлиенте
Процедура Проверить(Команда)
	
	ПроверкаПройдена = Ложь;
	
	Если Не ПустаяСтрока(Промокод) Тогда
		ПроверкаПройдена = ПоПромокодуЕстьМероприятие(Промокод,Подразделение);
	ИначеЕсли Не ПустаяСтрока(НомерТелефона) Тогда
		ПроверкаПройдена = ПоПартнеруЕстьМероприятие(НомерТелефона);
	КонецЕсли;
	
	Если ПроверкаПройдена Тогда
		Сообщить("Промокод найден и применен!"); 
		Закрыть(Новый Структура("Промокод,ПривелКлиент",Промокод,Партнер));
	Иначе
		Сообщить("Действующая акция не найдена!");
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервереБезКонтекста 
Функция ПоПромокодуЕстьМероприятие(Промокод,Подразделение)
	
	Возврат Справочники.МаркетинговыеМероприятия.ПолучитьМероприятияПоОтборуПромокод(Промокод,Подразделение).Количество()>0;
	
КонецФункции

&НаСервереБезКонтекста 
Функция ПоПартнеруЕстьМероприятие(НомерТелефона)
	
	Партнер = АльтерраОбщегоНазначения.ПолучитьПартнераПоТелефону(НомерТелефона);
	
	Возврат Справочники.МаркетинговыеМероприятия.ПолучитьМероприятияПоОтборуПартнера(Партнер).Количество()>0;
	
КонецФункции

#КонецОбласти

