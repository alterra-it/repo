	
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	ДатаЗакрытияПериода_Инициализация();
КонецПроцедуры
		   
&НаКлиенте
Процедура Записать(Команда)
	Если ДатаЗакрытияПериода_Записать(ДатаЗакрытияПериода_НачалоПериода, ДатаЗакрытияПериода_КонецПериода) Тогда
		ЭтаФорма.Модифицированность = Ложь;
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ЗаписатьИЗакрыть(Команда)
	Если ДатаЗакрытияПериода_Записать(ДатаЗакрытияПериода_НачалоПериода, ДатаЗакрытияПериода_КонецПериода) Тогда
		Закрыть();
	КонецЕсли;
КонецПроцедуры

&НаСервере
Процедура ДатаЗакрытияПериода_Инициализация()
	ДатаЗакрытияПериода = Справочники.ТипПериод.ДатаЗакрытияПериода;
	ДатаЗакрытияПериода_НачалоПериода = ДатаЗакрытияПериода.НачалоПериода;
	ДатаЗакрытияПериода_КонецПериода = ДатаЗакрытияПериода.КонецПериода;
КонецПроцедуры

&НаСервереБезКонтекста
Функция ДатаЗакрытияПериода_Записать(НачалоПериода, КонецПериода)
	ДатаЗакрытияПериода = Справочники.ТипПериод.ДатаЗакрытияПериода.ПолучитьОбъект();
	ДатаЗакрытияПериода.НачалоПериода = НачалоПериода;
	ДатаЗакрытияПериода.КонецПериода = КонецПериода;
	
	Попытка
		ДатаЗакрытияПериода.Записать();
	Исключение       
		ОбщегоНазначения.СообщитьПользователю("Не удалось записать данные!");
		Возврат Ложь;
	КонецПопытки;    
	
	Возврат Истина;
КонецФункции


