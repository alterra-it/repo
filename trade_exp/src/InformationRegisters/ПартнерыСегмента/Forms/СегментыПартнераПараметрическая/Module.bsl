
&НаКлиенте
&Вместо("ВключитьУдалитьВСегментКлиент")

Процедура СПР_ВключитьУдалитьВСегментКлиент(Включить)
	
	ВыделенныеСтроки = Элементы.Сегменты.ВыделенныеСтроки;
	
	Если ВыделенныеСтроки = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если ВыделенныеСтроки.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	МассивОбрабатываемыхСегментов = Новый Массив;
	
	Для каждого ВыделеннаяСтрока Из ВыделенныеСтроки Цикл
		
		ДанныеСтроки = Элементы.Сегменты.ДанныеСтроки(ВыделеннаяСтрока);
		Если ДанныеСтроки = Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		Если ДанныеСтроки.ВходитВСегмент = Включить Тогда
			Продолжить;
		Иначе
			
			//Альтерра+ 
			///Запрет включения в сегмент
			Если не ПроверкиАльтерраСервер.РазрешенВыборСегментаКлиента() и не ПроверкиАльтерраСервер.МожноВыбратьЭтотСегмент(ДанныеСтроки.Сегмент) и Включить тогда
				   ТекстСообщения = "Вам запрещено включение партнера в сегмент "+ДанныеСтроки.Сегмент+".";
  	               ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстСообщения);
             Иначе		///Запрет включения в сегмент.
    			   МассивОбрабатываемыхСегментов.Добавить(ДанныеСтроки.Сегмент);
			   КонецЕсли;
			 //Альтерра  
			
		КонецЕсли;
		
	КонецЦикла;
	
	Если МассивОбрабатываемыхСегментов.Количество() > 0 Тогда
		
		ВключитьУдалитьВСегментСервер(МассивОбрабатываемыхСегментов, Включить);
		Если Включить Тогда
			Оповестить("ДобавлениеПартнераВСегмент");
		Иначе
			Оповестить("УдалениеПартнераИзСегмента");
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

