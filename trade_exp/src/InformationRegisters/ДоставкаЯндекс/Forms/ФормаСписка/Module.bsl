#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	ДатаНачала = ТекущаяДатаСеанса() - 24 * 60 * 60;
	ДатаОкончания = ТекущаяДатаСеанса();
	ОбновитьСписокСобытийДашборд();
	
	ДатаНачалаЗагрузки = ДатаНачала;
	ДатаОкончанияЗагрузки = ДатаНачала; 	
	ОбновитьСписокСобытий();
	
	Элементы.СтраницаСобытия.Видимость = РольДоступна("ПолныеПрава");  
	          

КонецПроцедуры

&НаСервере
Процедура ОбновитьСписокСобытийДашборд()
	
	Дашборд.Параметры.УстановитьЗначениеПараметра("ДатаНачала", НачалоДня(ДатаНачала));
	Дашборд.Параметры.УстановитьЗначениеПараметра("ДатаОкончания", НачалоДня(ДатаОкончания + 24 * 60 * 60));
	
КонецПроцедуры

&НаСервере
Процедура ОбновитьСписокСобытий()
	
	СписокСобытий.Параметры.УстановитьЗначениеПараметра("ДатаНачала", НачалоДня(ДатаНачалаЗагрузки));
	СписокСобытий.Параметры.УстановитьЗначениеПараметра("ДатаОкончания",  НачалоДня(ДатаОкончанияЗагрузки + 24 * 60 * 60));
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовФормы

&НаКлиенте
Процедура ДатаНачалаПриИзменении(Элемент)
	
	ОбновитьСписокСобытийДашборд();
	
КонецПроцедуры

&НаКлиенте
Процедура ДатаОкончанияПриИзменении(Элемент)
	
	ОбновитьСписокСобытийДашборд();

КонецПроцедуры

&НаКлиенте
Процедура ДатаНачалаЗагрузкиПриИзменении(Элемент)

	ОбновитьСписокСобытий();	
	
КонецПроцедуры

&НаКлиенте
Процедура ДатаОкончанияЗагрузкиПриИзменении(Элемент)
	
	ОбновитьСписокСобытий();

КонецПроцедуры

&НаКлиенте
Процедура ДашбордВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	Если Элемент.ТекущиеДанные <> Неопределено Тогда

	    Если Поле.ГиперссылкаЯчейки тогда
	      ЗапуститьПриложение(Элемент.ТекущиеДанные.Трекинг);
	      
		ИначеЕсли Поле.Имя = "ДашбордНомер" Тогда
			ОткрытьФорму("Документ.ЗаказНаДоставкуКлиенту.ФормаОбъекта", Новый Структура("Ключ", Элемент.ТекущиеДанные.ЗаказнаДоставку));
	
		ИначеЕсли Поле.Имя = "ДашбордНомерРейса" Тогда
			ОткрытьФорму("Документ.ЗаданиеНаПеревозкуРейс.ФормаОбъекта", Новый Структура("Ключ", Элемент.ТекущиеДанные.Рейс));
		КонецЕсли; 
	
    КонецЕсли;
    
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ПолучитьСобытия(Команда)

	ПолучитьСобытияДоставки();

КонецПроцедуры

#КонецОбласти

#Область МониторингДоставки

&НаСервере
Процедура ПолучитьСобытияДоставки()

	ПолучениеСобытийДоставки = ИнтеграцияЯндекс.ПолучитьСобытияДоставки(ДатаНачалаЗагрузки, ДатаОкончанияЗагрузки);
	Сообщить(ПолучениеСобытийДоставки.Сообщение);
	
КонецПроцедуры	

#КонецОбласти


