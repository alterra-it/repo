#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

&После("ПередЗаписью")
Процедура Док_ПередЗаписью(Отказ, Замещение)
	Для Каждого ТекЗапись Из ЭтотОбъект Цикл
		Если ТекЗапись.КритерийСовместимости = ТекЗапись.КритерийНесовместимости Тогда
			Отказ = Истина;
			ОбщегоНазначения.СообщитьПользователю("Критерий совместимости не должен быть равен критерию несовместимости!");
			Прервать;
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

#КонецЕсли
