
&НаКлиенте
Процедура Загрузить(Команда)
	ОткрытьФорму("РегистрСведений.СтоимостьДоставкиПоЗонам.Форма.ФормаЗагрузки");
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьВсеЗаписи(Команда)
	 ПоказатьВсеЗаписиСервер();
КонецПроцедуры

&НаСервере
Процедура ПоказатьВсеЗаписиСервер()
	
	ТекущаяТаблица = Список.ОсновнаяТаблица;
	
	Если ТекущаяТаблица = "РегистрСведений.СтоимостьДоставкиПоЗонам" Тогда
	    Список.ОсновнаяТаблица = "РегистрСведений.СтоимостьДоставкиПоЗонам.СрезПоследних";
	    Элементы.ФормаПоказатьВсеЗаписи.Заголовок = "Показать все записи";
	Иначе
	    Список.ОсновнаяТаблица = "РегистрСведений.СтоимостьДоставкиПоЗонам";
	    Элементы.ФормаПоказатьВсеЗаписи.Заголовок = "Показать последние записи"; 
	КонецЕсли;
КонецПроцедуры