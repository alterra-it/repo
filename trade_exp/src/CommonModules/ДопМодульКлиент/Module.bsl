
Процедура ПоказатьРезервы(ФормаОбъекта, ИспользоватьХарактеристикиНоменклатуры, ПодборТекущаяНоменклатура, ПараметрыФормы, Режим) Экспорт
	ИмяОбработки = "ПодборТоваровВДокументПродажи";
	ОткрытьФорму("Обработка."+ИмяОбработки+".Форма.ФормаРезервы", ПараметрыФормы, ФормаОбъекта);	
КонецПроцедуры

Процедура ОткрытьИзображение(ФормаОбъекта, ПараметрыФормы) Экспорт
	ОткрытьФорму("Обработка.ПодборТоваровВДокументПродажи.Форма.Изображение", ПараметрыФормы, ФормаОбъекта);	
КонецПроцедуры

Процедура НачалоВыбораМаркетинговогоМероприятия(Элемент,
	СтандартнаяОбработка,
	КаналПС) Экспорт
	
	СтандартнаяОбработка = Ложь;
	
	Если Не ЗначениеЗаполнено(КаналПС) Тогда
		Возврат;
	Иначе
		ОткрытьФорму (
		"Справочник.МаркетинговыеМероприятия.ФормаВыбора",Новый Структура("Отбор",
		Новый Структура("КаналПервичногоИнтереса",КаналПС)),
		Элемент
		);
	КонецЕсли;	
КонецПроцедуры

Процедура ПечатьДоговораMicrosoftWord(ПараметрКоманды, ПараметрыВыполненияКоманды) Экспорт
	
	Если ТипЗнч(ПараметрКоманды) = Тип("Массив") Тогда
		СоставДокументов = ПараметрКоманды;
	Иначе
		СоставДокументов = Новый Массив;
		СоставДокументов.Добавить(ПараметрКоманды);
	КонецЕсли;
	
	Состояние(НСтр("ru = 'Выполняется формирование печатных форм'"));
	
	ИмяМакета = "ПФ_DOC_Договор";
	ТипМакета = "doc";
	
	МакетИДанныеОбъекта = УправлениеПечатьюВызовСервера.МакетыИДанныеОбъектовДляПечати("Обработка.ПечатьДоговораВWord",
	ИмяМакета,
	СоставДокументов
	);
	
	ДвоичныеДанныеМакетов = МакетИДанныеОбъекта.Макеты.ДвоичныеДанныеМакетов;
	
	Секции = МакетИДанныеОбъекта.Макеты.ОписаниеСекций;
	
	Для Каждого Ссылка Из СоставДокументов Цикл
		
		ДанныеОбъекта = МакетИДанныеОбъекта.Данные[Ссылка][ИмяМакета];
		
		Попытка
			
			Макет = УправлениеПечатьюКлиент.ИнициализироватьМакетОфисногоДокумента(
			ДвоичныеДанныеМакетов[ИмяМакета], ТипМакета
			);

			НастройкиСтраницыМакета=Макет.НастройкиСтраницыМакета;

			ПечатнаяФорма=УправлениеПечатьюКлиент.ИнициализироватьПечатнуюФорму(ТипМакета,НастройкиСтраницыМакета,Макет);
			
									
			Область = УправлениеПечатьюКлиент.ОбластьМакета(Макет,Секции[ИмяМакета]["ВерхнийКолонтитул"]);
			УправлениеПечатьюКлиент.ПрисоединитьОбласть(ПечатнаяФорма, Область, Ложь);
		    			
			Область = УправлениеПечатьюКлиент.ОбластьМакета(Макет,Секции[ИмяМакета]["Заголовок"]);
			УправлениеПечатьюКлиент.ПрисоединитьОбласть(ПечатнаяФорма, Область, Ложь);
			УправлениеПечатьюКлиент.ЗаполнитьПараметры(ПечатнаяФорма, ДанныеОбъекта);
			
			Если ДанныеОбъекта.ВидДоговора = ПредопределенноеЗначение("Перечисление.ВидДоговора.ФедеральныйБюджет")
				Или ДанныеОбъекта.ВидДоговора = ПредопределенноеЗначение("Перечисление.ВидДоговора.МестныйБюджет") Тогда
				Область = УправлениеПечатьюКлиент.ОбластьМакета(Макет,Секции[ИмяМакета]["Бюджет"]);
				УправлениеПечатьюКлиент.ПрисоединитьОбласть(ПечатнаяФорма, Область, Ложь);
				УправлениеПечатьюКлиент.ЗаполнитьПараметры(ПечатнаяФорма, ДанныеОбъекта);
			ИначеЕсли ДанныеОбъекта.ВидДоговора = ПредопределенноеЗначение("Перечисление.ВидДоговора.Предоплата") Тогда 
				Область = УправлениеПечатьюКлиент.ОбластьМакета(Макет,Секции[ИмяМакета]["Предоплата"]);
				УправлениеПечатьюКлиент.ПрисоединитьОбласть(ПечатнаяФорма, Область, Ложь);
				УправлениеПечатьюКлиент.ЗаполнитьПараметры(ПечатнаяФорма, ДанныеОбъекта);
			ИначеЕсли ДанныеОбъекта.ВидДоговора = ПредопределенноеЗначение("Перечисление.ВидДоговора.Отсрочка") Тогда 
				Область = УправлениеПечатьюКлиент.ОбластьМакета(Макет,Секции[ИмяМакета]["Отсрочка"]);
				УправлениеПечатьюКлиент.ПрисоединитьОбласть(ПечатнаяФорма, Область, Ложь);
				УправлениеПечатьюКлиент.ЗаполнитьПараметры(ПечатнаяФорма, ДанныеОбъекта);
			ИначеЕсли ДанныеОбъекта.ВидДоговора = ПредопределенноеЗначение("Перечисление.ВидДоговора.Транзитный") Тогда 
				Область = УправлениеПечатьюКлиент.ОбластьМакета(Макет,Секции[ИмяМакета]["Транзитный"]);
				УправлениеПечатьюКлиент.ПрисоединитьОбласть(ПечатнаяФорма, Область, Ложь);
				УправлениеПечатьюКлиент.ЗаполнитьПараметры(ПечатнаяФорма, ДанныеОбъекта);
	      	КонецЕсли;
			
			Если ДанныеОбъекта.ЮрФизЛицо = ПредопределенноеЗначение("Перечисление.ЮрФизЛицо.ЮрЛицо") Тогда
				Область = УправлениеПечатьюКлиент.ОбластьМакета(Макет,Секции[ИмяМакета]["ПодвалЮрЛицо"]);
				УправлениеПечатьюКлиент.ПрисоединитьОбласть(ПечатнаяФорма, Область, Ложь);
				УправлениеПечатьюКлиент.ЗаполнитьПараметры(ПечатнаяФорма, ДанныеОбъекта);
			ИначеЕсли ДанныеОбъекта.ЮрФизЛицо = ПредопределенноеЗначение("Перечисление.ЮрФизЛицо.ИндивидуальныйПредприниматель") Тогда 
				Область = УправлениеПечатьюКлиент.ОбластьМакета(Макет,Секции[ИмяМакета]["ПодвалИП"]);
				УправлениеПечатьюКлиент.ПрисоединитьОбласть(ПечатнаяФорма, Область, Ложь);
				УправлениеПечатьюКлиент.ЗаполнитьПараметры(ПечатнаяФорма, ДанныеОбъекта);
			КонецЕсли;

			ВставитьРазрывСтраницы(ПечатнаяФорма);
			
			Область = УправлениеПечатьюКлиент.ОбластьМакета(Макет,Секции[ИмяМакета]["ЛистСогласования"]);
			УправлениеПечатьюКлиент.ПрисоединитьОбласть(ПечатнаяФорма, Область, Ложь);
			УправлениеПечатьюКлиент.ЗаполнитьПараметры(ПечатнаяФорма, ДанныеОбъекта);
		
			Область = УправлениеПечатьюКлиент.ОбластьМакета(Макет,Секции[ИмяМакета]["НижнийКолонтитул"]);
			УправлениеПечатьюКлиент.ПрисоединитьОбласть(ПечатнаяФорма, Область, Ложь);
            			
			УправлениеПечатьюКлиент.ПоказатьДокумент(ПечатнаяФорма);
			
		Исключение
			
			ОбщегоНазначенияКлиентСервер.СообщитьПользователю(КраткоеПредставлениеОшибки(ИнформацияОбОшибке()));
			
			УправлениеПечатьюКлиент.ОчиститьСсылки(ПечатнаяФорма);
			УправлениеПечатьюКлиент.ОчиститьСсылки(Макет);
			
			Возврат;
			
		КонецПопытки;
		
		УправлениеПечатьюКлиент.ОчиститьСсылки(ПечатнаяФорма, Ложь);
		УправлениеПечатьюКлиент.ОчиститьСсылки(Макет);
		
	КонецЦикла;
	
	Состояние(НСтр("ru = 'Формирование печатных форм завершено'"));
	
КонецПроцедуры

Процедура ВставитьРазрывСтраницы(знач Handler) Экспорт
	
	ActiveDocument = Handler.COMСоединение.ActiveDocument;
	ПозицияОкончанияДокумента = ActiveDocument.Range().End;
	ActiveDocument.Range(ПозицияОкончанияДокумента-1, ПозицияОкончанияДокумента-1).InsertBreak();
    		
КонецПроцедуры

Процедура УстановитьТекущийЭлементСписокСкладов(Форма) Экспорт	
	Форма.ТекущийЭлемент = Форма.Элементы.ОстаткиТоваров;	
КонецПроцедуры

Процедура НапечататьПакетНаПринтер(Объект, ИмяПринтера=Неопределено)  Экспорт
	
	Если ЗначениеЗаполнено(Объект.Ссылка) Тогда
		
		ПараметрКоманды= Новый Массив;
		ПараметрКоманды.Добавить(Объект.Ссылка);
		ПараметрыПечати= Новый Структура;
		ПараметрыПечати.Вставить("ВыводитьСкидки", ИСТИНА);
		ПараметрыПечати.Вставить("ИмяПринтера", ИмяПринтера);
		
		Если УправлениеПечатьюКлиент.ПроверитьДокументыПроведены(ПараметрКоманды) Тогда
			УправлениеПечатьюКлиент.ВыполнитьКомандуПечатиНаПринтер(
			"Обработка.ПечатьФормДополнительно",
			"ПечатьТоварногоЧека",
			ПараметрКоманды,
			ПараметрыПечати);
		КонецЕсли;
		
		Если УправлениеПечатьюКлиент.ПроверитьДокументыПроведены(ПараметрКоманды) Тогда
			УправлениеПечатьюКлиент.ВыполнитьКомандуПечатиНаПринтер(
			"Обработка.ПечатьФормДополнительно",
			"ПечатьТНСекцииРеализацияНов",
			ПараметрКоманды,
			ПараметрыПечати);
		КонецЕсли;
		
	Иначе
		Сообщить("Перед печатью необходимо провести документы!");
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура УстановитьОтметкуКассы(Форма, Пользователь) Экспорт
	    	
	Если Форма.Объект.Кассир.Пустая() Тогда
		Форма.Объект.Кассир = Пользователь;
		Попытка
			Форма.Объект.ДатаОтметкиКасса = ТекущаяДата();
		Исключение 
		КонецПопытки;
	Иначе
		Форма.Объект.Кассир = "";
	Конецесли;
	
	Форма.Модифицированность = Истина;
	
	ОбщегоНазначенияУТКлиент.ЗаписатьИЗакрыть(Форма, Истина);
	
КонецПроцедуры

#Область РаботаСоСлипЧеками  

Процедура НапечататьПоследнийСлипЧек(УникальныйИдентификатор, ИдентификаторУстройства) Экспорт
	
	ОчиститьСообщения();
	
	СтрокаСлипЧека = "";
	
	ПодключаемоеОборудование = МенеджерОборудованияКлиент.ПодключаемоеОборудование();
	Если Не ПодключаемоеОборудование.Свойство("ПоследнийСлипЧек", СтрокаСлипЧека)
		Или ТипЗнч(СтрокаСлипЧека) <> Тип("Строка") 
		Или ПустаяСтрока(СтрокаСлипЧека) Тогда
		
		ОбщегоНазначенияКлиент.СообщитьПользователю(
			НСтр("ru='Слип-чек отсутствует.
			         |Возможно для данного сеанса еще не выполнялась эквайринговая операция.'"));
		
		Возврат;
		
	КонецЕсли;
	
	ОборудованиеПодключено = МенеджерОборудованияУТКлиент.ОборудованиеПодключено(ИдентификаторУстройства);
	Если ОборудованиеПодключено Тогда
		
		УИДЗамераПечатьСлипЧека = ОценкаПроизводительностиКлиент.ЗамерВремени("Документ.ЧекККМ.Форма.ФормаДокументаРМК.НапечататьПоследнийСлипЧек");
		
		ОповещениеПриЗавершении = Новый ОписаниеОповещения("ПослеЗавершенияПечатиСлипЧека", ЭтотОбъект);
		
		ПараметрыОперации = ОборудованиеЧекопечатающиеУстройстваКлиентСервер.ПараметрыПечатиТекста(СтрокаСлипЧека);
		
		ОборудованиеЧекопечатающиеУстройстваКлиент.НачатьПечатьТекста(
			ОповещениеПриЗавершении,
			УникальныйИдентификатор,
			ИдентификаторУстройства,
			ПараметрыОперации);
		
	Иначе
		
		ОбщегоНазначенияКлиент.СообщитьПользователю(
			НСтр("ru = 'Фискальное устройство не подключено.'"));
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ПослеЗавершенияПечатиСлипЧека(РезультатВыполнения, ДополнительныеПараметры) Экспорт
	
	Если Не РезультатВыполнения.Результат Тогда
		
		ТекстСообщения = СтрШаблон(
			НСтр("ru = 'При печати слип-чека возникла ошибка:
			           |""%1"".'"),
			РезультатВыполнения.ОписаниеОшибки);
		
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстСообщения);
		
	КонецЕсли;
	
КонецПроцедуры 

Функция ДобавитьВСлипЧекФИОКлиента(ТекстЧека) Экспорт  
	
	Если Не ЗначениеЗаполнено(ТекстЧека) Тогда
		Возврат ТекстЧека;
	КонецЕсли;
	
	ТекстРезультат = "";     
	ТекстВозврат = Ложь; 
	ОбеСтрокиНайдены = Ложь; // "подпись клиента" и "подпись кассира"   
	
	КолСтрок = СтрЧислоСтрок(ТекстЧека);
	Для НомерСтроки = 1 По КолСтрок Цикл
		Строка = СтрПолучитьСтроку(ТекстЧека, НомерСтроки);
		ТекстРезультат = ?(ЗначениеЗаполнено(ТекстРезультат), ТекстРезультат + Символы.ПС, "") + Строка;
		
		Если СтрНайти(НРег(Строка), "подпись") <> 0 
			И СтрНайти(НРег(Строка), "клиента") <> 0 Тогда 
						
			ТекстРезультат_ = "";
			Для НомерСтроки_ = НомерСтроки + 1 По КолСтрок Цикл
				ТекстРезультат_ = ТекстРезультат_ + Символы.ПС + СтрПолучитьСтроку(ТекстЧека, НомерСтроки_);
				
				Если СтрНайти(НРег(ТекстРезультат_), "подпись") <> 0 
					И СтрНайти(НРег(ТекстРезультат_), "кассира") <> 0 Тогда  
					
					ТекстРезультат_ = СтрЗаменить(ТекстРезультат_, "подпись", "ФИО");
					ТекстРезультат_ = СтрЗаменить(ТекстРезультат_, "Подпись", "ФИО");
					ТекстРезультат_ = СтрЗаменить(ТекстРезультат_, "ПОДПИСЬ", "ФИО");
					
					ТекстРезультат_ = СтрЗаменить(ТекстРезультат_, "кассира", "клиента");
					ТекстРезультат_ = СтрЗаменить(ТекстРезультат_, "Кассира", "Клиента");
					ТекстРезультат_ = СтрЗаменить(ТекстРезультат_, "КАССИРА", "КЛИЕНТА");
					
					ТекстРезультат = ТекстРезультат + ТекстРезультат_;
					ОбеСтрокиНайдены = Истина;
					Прервать;
				КонецЕсли; 
			КонецЦикла;
		ИначеЕсли СокрЛП(НРег(Строка)) = "возврат" Тогда
			ТекстВозврат = Истина;
		КонецЕсли;  
	КонецЦикла;  
	
	Возврат ?(ТекстВозврат И ОбеСтрокиНайдены, ТекстРезультат, ТекстЧека);
	
КонецФункции

#КонецОбласти

#Область НовыйРасчетОтклоненияВДокументеПересчетТоваров              

Процедура НовыйРасчетОтклонения_Табл(Товары, ИспользуетсяНовыйРасчетОтклонения) Экспорт
	Для Каждого СтрТабл Из Товары Цикл
		НовыйРасчетОтклонения_СтрТабл(СтрТабл, ИспользуетсяНовыйРасчетОтклонения);                                    
	КонецЦикла;	                                                                        
КонецПроцедуры

Процедура НовыйРасчетОтклонения_СтрТабл(СтрТабл, ИспользуетсяНовыйРасчетОтклонения) Экспорт  
	Если СтрТабл <> Неопределено Тогда 
		Если ИспользуетсяНовыйРасчетОтклонения Тогда
			СтрТабл.КоличествоУпаковокОтклонение = СтрТабл.КоличествоУпаковокФакт - СтрТабл.КоличествоПоУчетуВЗонеХранения;
		Иначе          
			СтрТабл.КоличествоУпаковокОтклонение = СтрТабл.КоличествоУпаковокФакт - СтрТабл.КоличествоУпаковок;
		КонецЕсли;
		
		СтрТабл.Расхождение = СтрТабл.КоличествоУпаковокОтклонение <> 0;
	КонецЕсли;
КонецПроцедуры

Процедура ПересчитатьФакт(СтрТабл, ИспользуетсяНовыйРасчетОтклонения, КоэффициентУпаковки) Экспорт  
	Если СтрТабл <> Неопределено Тогда 
		Если ИспользуетсяНовыйРасчетОтклонения Тогда
			СтрТабл.КоличествоУпаковокФакт = СтрТабл.КоличествоУпаковокОтклонение + СтрТабл.КоличествоПоУчетуВЗонеХранения;
			СтрТабл.КоличествоФакт = СтрТабл.КоличествоУпаковокОтклонение + (КоэффициентУпаковки * СтрТабл.КоличествоПоУчетуВЗонеХранения);
		КонецЕсли;
	КонецЕсли;
КонецПроцедуры

#КонецОбласти

#Область ОбщиеМетодыДляФормПриходныхОрдеровТСД

Процедура ОчиститьСтрокиСНулевымКоличеством(Товары, СтатусПринят, КэшированныеЗначения) Экспорт
	
	Если СтатусПринят Тогда
		РазделитьСтрокиСКоличетсвомБольшеПлана(Товары, КэшированныеЗначения);
		УдаляемыеСтроки = Товары.НайтиСтроки(Новый Структура("КоличествоУпаковок", 0));		
	Иначе
		УдаляемыеСтроки = Товары.НайтиСтроки(Новый Структура("КоличествоПлан, КоличествоУпаковок", 0, 0));
	КонецЕсли;
	
	Для Каждого Стр Из УдаляемыеСтроки Цикл
		Товары.Удалить(Стр);
	КонецЦикла;
	
КонецПроцедуры

Процедура РазделитьСтрокиСКоличетсвомБольшеПлана(Товары, КэшированныеЗначения)
	
	ПустаяСсылка = ПредопределенноеЗначение("Справочник.Назначения.ПустаяСсылка");
	
	Для Каждого Стр Из Товары Цикл
		Если ЗначениеЗаполнено(Стр.Назначение) И ЗначениеЗаполнено(Стр.КоличествоПлан) И Стр.КоличествоУпаковок > Стр.КоличествоПлан Тогда
			НС = Товары.Добавить();
			ЗаполнитьЗначенияСвойств(НС, Стр);   
			
			НС.Назначение = ПустаяСсылка;
			НС.КоличествоПлан = 0;
			НС.КоличествоУпаковок = Стр.КоличествоУпаковок - Стр.КоличествоПлан;
			НС.Количество = НС.КоличествоУпаковок;
			НС.Расхождение = Истина;
			ПакетнаяОбработкаТабличнойЧастиКлиент.ОбработатьСтрокуТЧ(НС, Новый Структура("ПересчитатьКоличествоЕдиниц"), КэшированныеЗначения);
			
			Стр.КоличествоУпаковок = Стр.КоличествоПлан;
			Стр.Количество = Стр.КоличествоУпаковок;
			НС.Расхождение = Ложь;
			ПакетнаяОбработкаТабличнойЧастиКлиент.ОбработатьСтрокуТЧ(Стр, Новый Структура("ПересчитатьКоличествоЕдиниц"), КэшированныеЗначения);
			
			РазделитьСтрокиСКоличетсвомБольшеПлана(Товары, КэшированныеЗначения);
			Возврат;
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти



