
&Вместо("УправлениеДоступностью")
Процедура Док_УправлениеДоступностью(Форма)
	Если Форма.Объект.Статус = ПредопределенноеЗначение("Перечисление.СтатусыАктаОРасхождениях.Претензия") Тогда
		Форма.Объект.Статус = ПредопределенноеЗначение("Перечисление.СтатусыАктаОРасхождениях.КВыполнению");
		ПродолжитьВызов(Форма);	   
		Форма.Объект.Статус = ПредопределенноеЗначение("Перечисление.СтатусыАктаОРасхождениях.Претензия");
	Иначе
		ПродолжитьВызов(Форма);	
	КонецЕсли;	
	
КонецПроцедуры

Функция Док_ТекстЗапросаОбъединить() Экспорт
	
	Возврат "
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|";
	
КонецФункции


&После("РассчитатьИтоговыеПоказателиФормы")
Процедура Док_РассчитатьИтоговыеПоказателиФормы(Форма)
	Если Форма.ИмяФормы = "Документ.АктОРасхожденияхПослеПриемки.Форма.ФормаДокумента" Тогда
		
		ИтогСуммаСНДСПоДокументу = Форма.Объект.Товары.Итог("СуммаСНДСПоДокументу");
		
		Если Форма.Расхождения < 0 И ИтогСуммаСНДСПоДокументу <> 0 Тогда
			ПроцентУбыли = Форма.Расхождения / ИтогСуммаСНДСПоДокументу * (-100);
		Иначе
			ПроцентУбыли = 0;
		КонецЕсли;
		
		Форма.ПроцентУбыли = ПроцентУбыли;
	КонецЕсли;
КонецПроцедуры

