
Процедура ПоследовательностьПоСебестоимостиПередЗаписью(Источник, Отказ, Замещение) Экспорт
	ВремяНачала=ОценкаПроизводительности.НачатьЗамерВремени();	
    Документ=Источник.Отбор.Регистратор.Значение;
    режим=ОпределитьРежимПроведения(Источник);
	РегистрыСведений.ГраницаПоследовательностиСебестоимости.УстановитьГраницуПоследовательности(Источник,режим); 
	ОценкаПроизводительности.ЗакончитьЗамерВремени("Отразить последовательность",времяначала,,строка(Документ));
	
КонецПроцедуры      

// Режим :
// 1 - проведение с определением границы
// 2 - отмена проведения
// 3 - установка границы без анализа
 Функция ОпределитьРежимПроведения(Источник)
	 если Источник.ДополнительныеСвойства.Свойство("СвойстваДокумента") тогда
		если  Источник.ДополнительныеСвойства.СвойстваДокумента.РежимЗаписи=РежимЗаписиДокумента.ОтменаПроведения тогда
			Возврат 2;
		конецесли;	 
	 конецесли;	 
 	 возврат 1;
 
 КонецФункции // ОпределитьРежимПроведения()
 