
&После("ПередВыполнениемКомандыПечатиВнешнейПечатнойФормы")
Процедура Док_ПередВыполнениемКомандыПечатиВнешнейПечатнойФормы(ПечатаемыеОбъекты, СтандартнаяОбработка)
	СтруктураПроведен = Новый Структура("Проведен");
	Для каждого Объект ИЗ ПечатаемыеОбъекты Цикл
		ЗаполнитьЗначенияСвойств(СтруктураПроведен, Объект);
		Если НЕ ЗначениеЗаполнено(СтруктураПроведен.Проведен) Тогда
			СтандартнаяОбработка = Ложь;
			Возврат;
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры
