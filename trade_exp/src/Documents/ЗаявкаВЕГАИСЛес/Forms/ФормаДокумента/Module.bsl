&НаКлиенте
Процедура УстановитьВидимость()
	
	Если ПроверкаКлиента() = "ФизЛицо" тогда 
		ЭтаФорма.Элементы.ДляФИЗЛица.Видимость = Истина;
		ЭтаФорма.Элементы.ФормаДокументЗаявкаВЕГАИСЛесПечатьБланка.Видимость=Ложь;
		ЭтаФорма.Элементы.ФормаДокументЗаявкаВЕГАИСЛесПечатьТНЮЛ.Видимость= РейсИЛИЗаказЕсть();
		ЭтаФорма.Элементы.ДляЮрЛица.Видимость = Ложь;
	Иначе
		Если ПроверкаКлиента() = "ЮрЛицо" тогда 
		ЭтаФорма.Элементы.ДляЮрЛица.Видимость = Истина;
		ЭтаФорма.Элементы.ФормаДокументЗаявкаВЕГАИСЛесПечатьБланка.Видимость=Ложь;
		ЭтаФорма.Элементы.ФормаДокументЗаявкаВЕГАИСЛесПечатьТНЮЛ.Видимость=Истина;
		ЭтаФорма.Элементы.ДляФИЗЛица.Видимость = Ложь;
	Иначе
		ЭтаФорма.Элементы.ДляЮрЛица.Видимость = Ложь;
		ЭтаФорма.Элементы.ДляФИЗЛица.Видимость = Ложь;
		КонецЕсли;
	КонецЕсли;  
	
КонецПроцедуры

&НаСервере
Функция ПроверкаКлиента()
	
	Если ЗначениеЗаполнено(Объект.ТипКлиента) Тогда
		Если Объект.ТипКлиента = Перечисления.ЮридическоеФизическоеЛицо.ФизическоеЛицо Тогда
			Возврат "ФизЛицо";
		Иначе Возврат "ЮрЛицо";
		КонецЕсли;
	КонецЕсли; 
	
КонецФункции   

&НаСервере
Процедура ПриОткрытииНаСервере()  
	
	Если Объект.Статус = Истина Тогда
		ЭтаФорма.Элементы.ФормаЗанесеноВЕГАИС.Доступность = Ложь;
		ЭтаФОрма.Элементы.ГруппаЭлементыФормы.Доступность = Ложь;
	КонецЕсли;  
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	УстановитьВидимость(); 
	ПриОткрытииНаСервере();
	
КонецПроцедуры

&НаСервере
Процедура ЗанесеноВЕГАИСНаСервере()
	
	Если Объект.ТипКлиента = Перечисления.ЮридическоеФизическоеЛицо.ЮридическоеЛицо И НЕ ЗначениеЗаполнено(Объект.НомерДекларацииЮЛ)Тогда 
		Сообщить("Не заполнено поле «Номер декларации»");
		Возврат;
	КонецЕсли;
	
	Объект.Статус = Истина;	
	ЭтаФорма.Элементы.ФормаЗанесеноВЕГАИС.Доступность = Ложь;
    ЭтаФОрма.Элементы.ГруппаЭлементыФормы.Доступность = Ложь;

КонецПроцедуры

&НаКлиенте
Процедура ЗанесеноВЕГАИС(Команда)
	ЗанесеноВЕГАИСНаСервере();	
КонецПроцедуры

&НаСервере
Процедура ОбновитьНаСервере()	
    Документы.ЗаявкаВЕГАИСЛес.ЗаполнитьДанныеПоОснованию(Объект.Основание, Объект);
КонецПроцедуры

&НаКлиенте
Процедура Обновить(Команда)
	
	ОбновитьНаСервере();
	УстановитьВидимость(); 
	
КонецПроцедуры

&НаСервере
Процедура ОтменитьНаСервере()
	
	Объект.Статус = Ложь;	
	ЭтаФорма.Элементы.ФормаЗанесеноВЕГАИС.Доступность = Истина;
	ЭтаФОрма.Элементы.ГруппаЭлементыФормы.Доступность = Истина; 
	
КонецПроцедуры

&НаКлиенте
Процедура Отменить(Команда)
	ОтменитьНаСервере();
КонецПроцедуры

&НаСервере
Функция РейсИЛИЗаказЕсть()
	Возврат Документы.ЗаявкаВЕГАИСЛес.СуществуютРейсыИлиЗаказыНаДоставку(Объект.Основание);
КонецФункции

