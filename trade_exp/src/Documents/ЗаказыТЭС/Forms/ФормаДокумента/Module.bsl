&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если ЗначениеЗаполнено(Параметры.ЗначениеКопирования.Автор) тогда
		Объект.Автор=ПараметрыСеанса.ТекущийПользователь;
		Объект.ДатаЗаявки=ТекущаяДата();
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Объект.Автор) тогда
		Объект.Автор=ПараметрыСеанса.ТекущийПользователь;
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Объект.ДатаЗаявки) тогда
		Объект.ДатаЗаявки=ТекущаяДата()
	КонецЕсли;
	
	ОснованиеДокумента = Объект.Основание;
	
	ВидимостьПоставкиНаСклад = Истина;

	Если ЗначениеЗаполнено(ОснованиеДокумента) Тогда
		ВидимостьПоставкиНаСклад = Не (Альтерра_СкладСервер.ИспользуетсяНоваяСхемаРаботыСПоставками(ОснованиеДокумента.Дата) ИЛИ Параметры.Ключ.Пустая());
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ГрузоотправительПриИзменении(Элемент)
	
	Если ЗначениеЗаполнено(Объект.Грузоотправитель) тогда
		ГрузоотправительИзменитьНаСервере();
	КонецЕсли;  
	
КонецПроцедуры 

&НаСервере
Процедура ГрузоотправительИзменитьНаСервере()
	
	Объект.АдресОфисаОтправления = ФормированиеПечатныхФорм.ОписаниеОрганизации(ФормированиеПечатныхФорм.СведенияОЮрФизЛице(Объект.Грузоотправитель, ТекущаяДата()), "ФактическийАдрес");
	Телефоны = ФормированиеПечатныхФорм.ОписаниеОрганизации(ФормированиеПечатныхФорм.СведенияОЮрФизЛице(Объект.Грузоотправитель, ТекущаяДата()), "Телефоны");
	Объект.ТелефоныОтправителя = Прав(Телефоны,СТРДлина(Телефоны)-6);

КонецПроцедуры

&НаКлиенте
Процедура ОбновитьПоОснованию(Команда)
	ОбновитьПоОснованиюНаСервере();
КонецПроцедуры 

&НаСервере
Процедура ОбновитьПоОснованиюНаСервере()
	Документы.ЗаказыТЭС.ЗаполнитьДанныеПоОснованию(Объект.Основание, Объект);
КонецПроцедуры

&НаКлиенте
Процедура Заказчик2ПриИзменении(Элемент)
	Объект.Грузополучатель = Объект.Заказчик;
КонецПроцедуры




