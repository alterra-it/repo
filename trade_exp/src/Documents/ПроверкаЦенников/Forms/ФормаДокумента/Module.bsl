&НаКлиенте
Перем МожноЗаписатьДокумент;

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	// СтандартныеПодсистемы.ПодключаемыеКоманды	
	ПодключаемыеКоманды.ПриСозданииНаСервере(ЭтотОбъект);
	// Конец СтандартныеПодсистемы.ПодключаемыеКоманды
	
	ОбщегоНазначенияУТ.НастроитьПодключаемоеОборудование(ЭтаФорма);
	
	УстановитьОграничениеДополнительногоСведения();
	ИнициализироватьКомпоновкуДанных(Ложь);
	
КонецПроцедуры

// СтандартныеПодсистемы.ПодключаемыеКоманды
&НаКлиенте
Процедура Подключаемый_ВыполнитьКоманду(Команда)
	ПодключаемыеКомандыКлиент.ВыполнитьКоманду(ЭтотОбъект, Команда, Объект);
КонецПроцедуры

&НаСервере
Процедура Подключаемый_ВыполнитьКомандуНаСервере(Контекст, Результат) Экспорт
	ПодключаемыеКоманды.ВыполнитьКоманду(ЭтотОбъект, Контекст, Объект, Результат);
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОбновитьКоманды()
	ПодключаемыеКомандыКлиентСервер.ОбновитьКоманды(ЭтотОбъект, Объект);
КонецПроцедуры
// Конец СтандартныеПодсистемы.ПодключаемыеКоманды

&НаСервере
Процедура УстановитьОграничениеДополнительногоСведения()
   Запрос = Новый Запрос;
   Запрос.Текст = "ВЫБРАТЬ РАЗЛИЧНЫЕ(Свойство) КАК Свойство
					|ИЗ РегистрСведений.ДополнительныеСведения
					|ГДЕ ТипЗначения(Объект) = ТИП(Справочник.Номенклатура)";
    Выборка = Запрос.Выполнить().Выбрать();
	
	ПараметрыВыбораДополнительногоСведения = Новый Массив();	
	
	ДоступныеДополнительныеСведения = Новый Массив();
	
	Пока Выборка.Следующий() Цикл
		ДоступныеДополнительныеСведения.Добавить(Выборка.Свойство);	
	КонецЦикла;
	
	Параметр = Новый ПараметрВыбора(
				"Отбор.Ссылка",
		    	    	Новый ФиксированныйМассив(ДоступныеДополнительныеСведения)
			);	
	
	ПараметрыВыбораДополнительногоСведения.Добавить(Параметр);
	
	Элементы.ДополнительноеСведениеКолонки.ПараметрыВыбора = Новый ФиксированныйМассив(ПараметрыВыбораДополнительногоСведения);
	
	ЗаполнитьЗначенияПоДопСведению();   
	
КонецПроцедуры

&НаСервере
Процедура ИнициализироватьКомпоновкуДанных(ВсегдаИспользоватьНастройкиПоУмолчанию)
	СхемаКомпоновкиДанных = Документы.ПроверкаЦенников.ПолучитьМакет("ОтборПроверкиПоСкладу");
	
	URLСхемы = ПоместитьВоВременноеХранилище(СхемаКомпоновкиДанных, Новый УникальныйИдентификатор());
	
	ИсточникНастроек = Новый ИсточникДоступныхНастроекКомпоновкиДанных(URLСхемы);
	
	ОтборПроверки.Инициализировать(ИсточникНастроек);
	
	ТекОбъект = РеквизитФормыВЗначение("Объект");
	ТекНастройки = ТекОбъект.ОтборПроверки.Получить();
	Если ТекНастройки = Неопределено
		Или ВсегдаИспользоватьНастройкиПоУмолчанию Тогда
		ОтборПроверки.ЗагрузитьНастройки(СхемаКомпоновкиДанных.НастройкиПоУмолчанию);
	Иначе
		ОтборПроверки.ЗагрузитьНастройки(ТекНастройки);
	КонецЕсли;
	
	ОтборПроверки.Восстановить(СпособВосстановленияНастроекКомпоновкиДанных.ПроверятьДоступность);
	
	УстановитьЗначениеПараметраНастроек(ОтборПроверки.Настройки, "Склад" , Объект.Склад);
	УстановитьЗначениеПараметраНастроек(ОтборПроверки.Настройки, "ВидЦены" , Объект.ВидЦены);
	УстановитьЗначениеПараметраНастроек(ОтборПроверки.Настройки, "Свойство" , Объект.ДополнительноеСведениеКолонки);
	//УстановитьЗначениеПараметраНастроек(ОтборПроверки.Настройки, "Помещение", Объект.Помещение);
	
КонецПроцедуры

&НаСервере
Процедура УстановитьЗначениеПараметраНастроек(Настройки, ИмяПараметра, Значение)
	
	Параметр = Настройки.ПараметрыДанных.Элементы.Найти(ИмяПараметра);
	
	Если Параметр <> Неопределено Тогда
		Параметр.Значение = Значение;
		Параметр.Использование = Истина;
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьСервер()
	
	УстановитьЗначениеПараметраНастроек(ОтборПроверки.Настройки, "ДатаОстатков", ?(ЗначениеЗаполнено(Объект.Дата), Объект.Дата, ТекущаяДатаСеанса()));
	УстановитьЗначениеПараметраНастроек(ОтборПроверки.Настройки, "Склад" , Объект.Склад);
	УстановитьЗначениеПараметраНастроек(ОтборПроверки.Настройки, "ВидЦены" , Объект.ВидЦены);
	УстановитьЗначениеПараметраНастроек(ОтборПроверки.Настройки, "Свойство" , Объект.ДополнительноеСведениеКолонки);  
	УстановитьЗначениеПараметраНастроек(ОтборПроверки.Настройки, "ЗначенияДопСведения" , ЗначенияДополнительногоСведения);
	Если ЗначенияДополнительногоСведения.Количество() = 0 Тогда 
		УстановитьЗначениеПараметраНастроек(ОтборПроверки.Настройки, "ОтборПоДопСведению" , Ложь);
	Иначе
		УстановитьЗначениеПараметраНастроек(ОтборПроверки.Настройки, "ОтборПоДопСведению" , Истина);
	КонецЕсли;
	
	СхемаКомпоновкиДанных = Документы.ПроверкаЦенников.ПолучитьМакет("ОтборПроверкиПоСкладу");
	
	//ТекстЗапроса = СхемаКомпоновкиДанных.НаборыДанных.ЗаполнениеПоОтборам.Запрос;
	//ТекстЗапроса = СтрЗаменить(ТекстЗапроса,
	//	"&ТекстЗапросаКоэффициентУпаковки",
	//	Справочники.УпаковкиЕдиницыИзмерения.ТекстЗапросаКоэффициентаУпаковки(
	//	"ТоварыВЯчейках.Упаковка", Неопределено));
	//СхемаКомпоновкиДанных.НаборыДанных.ЗаполнениеПоОтборам.Запрос = ТекстЗапроса;

	СегментыСервер.ВключитьОтборПоСегментуНоменклатурыВСКД(ОтборПроверки);
	
	КомпоновщикМакета = Новый КомпоновщикМакетаКомпоновкиДанных;
	МакетКомпоновки   = КомпоновщикМакета.Выполнить(СхемаКомпоновкиДанных, ОтборПроверки.ПолучитьНастройки(),,,Тип("ГенераторМакетаКомпоновкиДанныхДляКоллекцииЗначений"));
	
	ПроцессорКомпоновкиДанных = Новый ПроцессорКомпоновкиДанных;
	ПроцессорКомпоновкиДанных.Инициализировать(МакетКомпоновки);
	
	ПроцессорВывода = Новый ПроцессорВыводаРезультатаКомпоновкиДанныхВКоллекциюЗначений;
	Объект.Товары.Загрузить(ПроцессорВывода.Вывести(ПроцессорКомпоновкиДанных));
	
	//ЗаполнитьСлужебныеРеквизитыПоНоменклатуре();
	//ЗаполнитьПризнакРасхождение();
	//
	//НоменклатураСервер.ЗаполнитьСтатусыУказанияСерий(Объект, ПараметрыУказанияСерий);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	МенеджерОборудованияКлиент.НачатьПодключениеОборудованиеПриОткрытииФормы(Неопределено, ЭтаФорма, "СканерШтрихкода");
	МожноЗаписатьДокумент = Ложь;
	Если ВидПроверкиПоПереоценке() Тогда
		Элементы.ТоварыНетЦенника.Заголовок = "Нет актуального ценника";
	Иначе
		Элементы.ТоварыНетЦенника.Заголовок = "Нет ценника";
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура СкладПриИзменении(Элемент)
	Объект.Подразделение= АльтерраОбщегоНазначения.ЗначениеРеквизитаОбъекта(Объект.Склад, "Подразделение");
	Объект.ВидЦены= АльтерраОбщегоНазначения.ЗначениеРеквизитаОбъекта(Объект.Подразделение, "ЦенаПоКарте");
КонецПроцедуры

&НаКлиенте
Процедура ПодразделениеПриИзменении(Элемент)
	Объект.ВидЦены= АльтерраОбщегоНазначения.ЗначениеРеквизитаОбъекта(Объект.Подразделение, "ЦенаПоКарте");
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	// ПодключаемоеОборудование
	Если Источник = "ПодключаемоеОборудование" И ВводДоступен() Тогда
		Если ИмяСобытия = "ScanData" И МенеджерОборудованияУТКлиент.ЕстьНеобработанноеСобытие() Тогда
			ДанныеШтрихкодов= ПодключаемоеОборудованиеУТКлиент.ПреобразоватьДанныеСоСканераВМассив(Параметр);
			Если ДанныеШтрихкодов.Количество() > 0 Тогда
			Штрихкод= ДанныеШтрихкодов[0].Штрихкод;
			
			ТекущаяЦена= 0;
			НомерСтроки= 0;
			ДобавитьСтроку(Штрихкод, ТекущаяЦена, НомерСтроки);
			
			Если ТекущаяЦена <> 0 Тогда
				Текст= "Текущая цена: "+ТекущаяЦена+Символы.ПС+"Правильная цена на ценнике?";
				ДопПараметры= Новый Структура("НомерСтроки", НомерСтроки);
				ОповещениеПослеЗакрытияВопроса = Новый ОписаниеОповещения("ПослеЗакрытияВопроса", ЭтаФорма, ДопПараметры);
				ПоказатьВопрос(ОповещениеПослеЗакрытияВопроса, Текст, РежимДиалогаВопрос.ДаНет, 10, КодВозвратаДиалога.Да);
			Иначе
				ПоказатьПредупреждение(, "Не удалось найти номенклатуру по штрихкоду!", 5);
				КонецЕсли;
		Иначе
			ПоказатьПредупреждение(, "Штрихкод неверного формата!", 5);
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	// Конец ПодключаемоеОборудование
КонецПроцедуры

&НаСервере
Процедура ДобавитьСтроку(Штрихкод, ТекущаяЦена, НомерСтроки)
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	ШтрихкодыНоменклатуры.Номенклатура КАК Номенклатура,
		|	ЦеныНоменклатурыСрезПоследних.Цена КАК ЦенаТекущая,
		|	ШтрихкодыНоменклатуры.Штрихкод КАК Штрихкод
		|ИЗ
		|	РегистрСведений.ШтрихкодыНоменклатуры КАК ШтрихкодыНоменклатуры
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ЦеныНоменклатуры.СрезПоследних(&ТекДата, ВидЦены = &ВидЦены) КАК ЦеныНоменклатурыСрезПоследних
		|		ПО ШтрихкодыНоменклатуры.Номенклатура = ЦеныНоменклатурыСрезПоследних.Номенклатура
		|			И ШтрихкодыНоменклатуры.Характеристика = ЦеныНоменклатурыСрезПоследних.Характеристика
		|ГДЕ
		|	ШтрихкодыНоменклатуры.Штрихкод = &Штрихкод";
	
	Запрос.УстановитьПараметр("ВидЦены", Объект.ВидЦены);
	Запрос.УстановитьПараметр("Штрихкод", Штрихкод);
	Запрос.УстановитьПараметр("ТекДата", ТекущаяДата());
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Выборка = РезультатЗапроса.Выбрать();
	
	Если Выборка.Следующий() Тогда
		НоваяСтрока= Объект.Товары.Добавить();
		ЗаполнитьЗначенияСвойств(НоваяСтрока, Выборка);
		ТекущаяЦена= Выборка.ЦенаТекущая; 
		НомерСтроки= НоваяСтрока.НомерСтроки;
	КонецЕсли;	
КонецПроцедуры

&НаКлиенте
Процедура ПослеЗакрытияВопроса(Результат, ДопПараметры) Экспорт
	ТекСтрока= Объект.Товары[ДопПараметры.НомерСтроки-1];
	Если Результат = КодВозвратаДиалога.Да ИЛИ Результат = КодВозвратаДиалога.Таймаут Тогда
		ТекСтрока.ЕстьРасхождения= Ложь;
	Иначе
		ТекСтрока.ЕстьРасхождения= Истина;
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьПечатьЦенников()
	ФормаПечатиЦенников= ПолучитьФорму("Обработка.ПечатьЭтикетокИЦенников.Форма",, ЭтаФорма);
	ФормаЦенниковОбъект= ФормаПечатиЦенников.Объект;
	
	ЗаполнитьЗначенияСвойств(ФормаЦенниковОбъект, Объект,"Организация,Подразделение,Склад");
	
	ЗаполнитьТовары(ФормаЦенниковОбъект);
	
	КопироватьДанныеФормы(ФормаЦенниковОбъект, ФормаПечатиЦенников.Объект);
	
	ФормаПечатиЦенников.Открыть();
КонецПроцедуры

&НаКлиенте
Процедура ПечатьЦенников(Команда)
	
	Если ЭтотОбъект.Модифицированность Или Не ЗначениеЗаполнено(Объект.Ссылка) Тогда
		ПоказатьВопрос(Новый ОписаниеОповещения("ОткрытьПечатьЦенниковЗавершение", ЭтотОбъект), НСтр("ru = 'Перед печатью ценников документ будет записан. Продолжить?'"), РежимДиалогаВопрос.ДаНет);
		Возврат;
	КонецЕсли;
	
	ОткрытьПечатьЦенников();
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьПечатьЦенниковЗавершение(РезультатВопроса, ДополнительныеПараметры) Экспорт
    
    Ответ = РезультатВопроса;
    Если Ответ = КодВозвратаДиалога.Да Тогда
        ЭтотОбъект.Записать();
    Иначе
        Возврат;
    КонецЕсли;
    
    ОткрытьПечатьЦенников();

КонецПроцедуры

&НаСервере
Процедура ЗаполнитьТовары(ФормаЦенниковОбъект)
	ФормаЦенниковОбъект.ВидЦены= Объект.Подразделение.ЦенаБезКарты;
	ФормаЦенниковОбъект.ВидЦеныДополнительно= Объект.Подразделение.ЦенаПоКарте;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	ПроверкаЦенниковТовары.Номенклатура КАК Номенклатура,
		|	ЦеныНоменклатурыСрезПоследнихБезКарты.Цена КАК ЦенаБезКарты,
		|	ЦеныНоменклатурыСрезПоследнихПоКарте.Цена КАК ЦенаПоКарте,
		|	ЦеныНоменклатурыСрезПоследних.Период КАК ДатаПоследнегоИзмененияЦены,
		|	МАКСИМУМ(ШтрихкодыНоменклатуры.Штрихкод) КАК Штрихкод
		|ИЗ
		|	Документ.ПроверкаЦенников.Товары КАК ПроверкаЦенниковТовары
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ЦеныНоменклатуры.СрезПоследних(&ТекДата, ВидЦены = &ВидЦеныБезКарты) КАК ЦеныНоменклатурыСрезПоследнихБезКарты
		|		ПО ПроверкаЦенниковТовары.Номенклатура = ЦеныНоменклатурыСрезПоследнихБезКарты.Номенклатура
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ЦеныНоменклатуры.СрезПоследних(&ТекДата, ВидЦены = &ВидЦеныПоКарте) КАК ЦеныНоменклатурыСрезПоследнихПоКарте
		|		ПО ПроверкаЦенниковТовары.Номенклатура = ЦеныНоменклатурыСрезПоследнихПоКарте.Номенклатура
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ШтрихкодыНоменклатуры КАК ШтрихкодыНоменклатуры
		|		ПО ПроверкаЦенниковТовары.Номенклатура = ШтрихкодыНоменклатуры.Номенклатура
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ЦеныНоменклатуры.СрезПоследних(КОНЕЦПЕРИОДА(&ТекДата, ДЕНЬ), ВидЦены = &ВидЦены) КАК ЦеныНоменклатурыСрезПоследних
		|		ПО ПроверкаЦенниковТовары.Номенклатура = ЦеныНоменклатурыСрезПоследних.Номенклатура
		|			И (ЦеныНоменклатурыСрезПоследних.Характеристика = ЗНАЧЕНИЕ(Справочник.ХарактеристикиНоменклатуры.ПустаяСсылка))
		|ГДЕ
		|	ПроверкаЦенниковТовары.ЕстьРасхождения
		|	И ПроверкаЦенниковТовары.Ссылка = &Ссылка
		|
		|СГРУППИРОВАТЬ ПО
		|	ПроверкаЦенниковТовары.Номенклатура,
		|	ЦеныНоменклатурыСрезПоследнихБезКарты.Цена,
		|	ЦеныНоменклатурыСрезПоследнихПоКарте.Цена,
		|	ЦеныНоменклатурыСрезПоследних.Период";
	
	Запрос.УстановитьПараметр("ВидЦеныБезКарты", Объект.Подразделение.ЦенаБезКарты);
	Запрос.УстановитьПараметр("ВидЦеныПоКарте", Объект.Подразделение.ЦенаПоКарте);
	Запрос.УстановитьПараметр("ТекДата", ТекущаяДата());
	Запрос.УстановитьПараметр("Ссылка", Объект.Ссылка);
	Запрос.УстановитьПараметр("ВидЦены", Объект.ВидЦены);
	
	РезультатЗапроса = Запрос.Выполнить();
	Выборка = РезультатЗапроса.Выбрать();
	
	Пока Выборка.Следующий() Цикл
		НоваяСтрока= ФормаЦенниковОбъект.Товары.Добавить();
		НоваяСтрока.Номенклатура= Выборка.Номенклатура;
		НоваяСтрока.Упаковка= Выборка.Номенклатура.ЕдиницаИзмерения;
		НоваяСтрока.Цена= Выборка.ЦенаБезКарты;
		НоваяСтрока.ЦенаДополнительно= Выборка.ЦенаПоКарте;
		НоваяСтрока.ДатаПоследнегоИзмененияЦены= Выборка.ДатаПоследнегоИзмененияЦены;
		НоваяСтрока.Штрихкод= Выборка.Штрихкод;
		НоваяСтрока.КоличествоЦенников= 1;
		НоваяСтрока.КоличествоЭтикеток= 1;
	КонецЦикла;
КонецПроцедуры

&НаКлиенте
Процедура ТоварыНетЦенникаПриИзменении(Элемент)
	
	ТекущиеДанные = Элементы.Товары.ТекущиеДанные;
	
	Если ТекущиеДанные.НетЦенника Тогда
		ТекущиеДанные.ЕстьРасхождения = ТекущиеДанные.НетЦенника;
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура Заполнить(Команда)
	//Если Не СкладПомещениеЗаполнены() Тогда
	//	Возврат;
	//КонецЕсли;
	
	Если Объект.Товары.Количество() > 0 Тогда
		Ответ = Неопределено;

		ПоказатьВопрос(Новый ОписаниеОповещения("ЗаполнитьЗавершение", ЭтотОбъект), НСтр("ru = 'Перед заполнением список товаров будет очищен. Продолжить?'"), РежимДиалогаВопрос.ДаНет);
        Возврат;
	КонецЕсли;
	
	Если Объект.ВидПроверки=предопределенноезначение("Перечисление.ВидПроверкиЦенников.ПоПлану") Тогда
		ЗаполнитьСервер();
	Иначе
		ЗаполнитьПоПереоценке();
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ЗаполнитьЗавершение(РезультатВопроса, ДополнительныеПараметры) Экспорт
    
    Ответ = РезультатВопроса;
    Если Ответ = КодВозвратаДиалога.Да Тогда
        Объект.Товары.Очистить();
    Иначе
        Возврат;
    КонецЕсли;
    
    Если ВидПроверкиПоПлану() Тогда
		ЗаполнитьСервер();
	Иначе
		ЗаполнитьПоПереоценке();
	КонецЕсли;

КонецПроцедуры

&НаСервереБезКонтекста
Процедура ТоварыПередОкончаниемРедактированияНаСервере(ВидЦены, ДополнительноеСведениеКолонки, Номенклатура, ЦенаТекущая, ДополнительноеСведение)
	Запрос = Новый Запрос;
	Запрос.Текст =			
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	ЦеныНоменклатурыСрезПоследних.Цена КАК ЦенаТекущая,
		|	ДополнительныеСведения.Значение КАК ДополнительноеСведение		
		|ИЗ
		|	РегистрСведений.ЦеныНоменклатуры.СрезПоследних(&ТекДата, ВидЦены = &ВидЦены) КАК ЦеныНоменклатурыСрезПоследних
		|		
		|	ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ДополнительныеСведения
		|		ПО ЦеныНоменклатурыСрезПоследних.Номенклатура.Ссылка = ДополнительныеСведения.Объект 
		|        	И ДополнительныеСведения.Свойство = &Свойство
		|ГДЕ
		|	ЦеныНоменклатурыСрезПоследних.Номенклатура = &Номенклатура";
		
	Запрос.УстановитьПараметр("ВидЦены", ВидЦены);
	Запрос.УстановитьПараметр("ТекДата", ТекущаяДата());
	Запрос.УстановитьПараметр("Свойство", ДополнительноеСведениеКолонки);
	Запрос.УстановитьПараметр("Номенклатура", Номенклатура);
	
	РезультатЗапроса = Запрос.Выполнить();
	Выборка = РезультатЗапроса.Выбрать();
	Если Выборка.Следующий() Тогда	
		ЦенаТекущая = Выборка.ЦенаТекущая;	
		ДополнительноеСведение = Выборка.ДополнительноеСведение;	
	Иначе
		ЦенаТекущая = 0;	
		ДополнительноеСведение = Справочники.ЗначенияСвойствОбъектов.ПустаяСсылка();	
	КонецЕсли;			
	
КонецПроцедуры

&НаКлиенте
Процедура ТоварыПередОкончаниемРедактирования(Элемент, НоваяСтрока, ОтменаРедактирования, Отказ)
	Перем ЦенаТекущая, ДополнительноеСведение;
	
	Если ЗначениеЗаполнено(Элемент.ТекущиеДанные.Номенклатура) Тогда
		ТоварыПередОкончаниемРедактированияНаСервере(Объект.ВидЦены, Объект.ДополнительноеСведениеКолонки, Элемент.ТекущиеДанные.Номенклатура, ЦенаТекущая, ДополнительноеСведение);
		
		Элемент.ТекущиеДанные.ЦенаТекущая = ЦенаТекущая;
		Элемент.ТекущиеДанные.ДополнительноеСведение = ДополнительноеСведение;
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ПередЗаписью(Отказ, ПараметрыЗаписи)
	
	Если Не МожноЗаписатьДокумент Тогда
		
		Для Каждого Строка Из Объект.Товары Цикл
			Если Не Строка.Проверено И Не Строка.ЕстьРасхождения Тогда
				Отказ = Истина;
				ПоказатьВопрос(Новый ОписаниеОповещения("ПроставитьРасхожденияЗавершение", ЭтотОбъект), НСтр("ru = 'Проставить расхождения у непроверенных позиций?'"), РежимДиалогаВопрос.ДаНетОтмена);
				Возврат;	
			КонецЕсли;
		КонецЦикла;
		
	Иначе
		МожноЗаписатьДокумент = Ложь;
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ПроставитьРасхожденияЗавершение(РезультатВопроса, ДополнительныеПараметры) Экспорт

	Если РезультатВопроса = КодВозвратаДиалога.Да Тогда
		
		Для Каждого Строка Из Объект.Товары Цикл
			Если Не Строка.Проверено И Не Строка.ЕстьРасхождения Тогда
				Строка.ЕстьРасхождения = Истина;
			КонецЕсли;
		КонецЦикла;
		
		МожноЗаписатьДокумент = Истина;
        ЭтотОбъект.Записать();
		
	ИначеЕсли РезультатВопроса = КодВозвратаДиалога.Нет Тогда
		МожноЗаписатьДокумент = Истина;
		ЭтотОбъект.Записать();
    КонецЕсли;
    

КонецПроцедуры

&НаСервере
Процедура ЗаполнитьЗначенияПоДопСведению()	
	Если ЗначениеЗаполнено(Объект.ДополнительноеСведениеКолонки) Тогда 
		Запрос = Новый Запрос; 
		Запрос.Текст = "ВЫБРАТЬ
						|	ЗначенияСвойствОбъектов.Ссылка КАК Ссылка
						|ИЗ
						|	Справочник.ЗначенияСвойствОбъектов КАК ЗначенияСвойствОбъектов
						|ГДЕ
						|	ЗначенияСвойствОбъектов.Владелец = &Владелец"; 
		
		Запрос.УстановитьПараметр("Владелец", Объект.ДополнительноеСведениеКолонки);  
		
		РезультатЗапроса = Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку(0); 
		Если ЗначениеЗаполнено(РезультатЗапроса) Тогда  
			ЗначенияДополнительногоСведения.ЗагрузитьЗначения(РезультатЗапроса);       
			Элементы.ЗначенияДополнительногоСведения.ВыбиратьТип = Ложь; 
			Возврат;
		КонецЕсли;
	КонецЕсли; 
	
	
	ЗначенияДополнительногоСведения.Очистить();
	Элементы.ЗначенияДополнительногоСведения.ВыбиратьТип = Истина;
	
КонецПроцедуры

&НаКлиенте
Процедура ДополнительноеСведениеКолонкиПриИзменении(Элемент) 
	ЗаполнитьЗначенияПоДопСведению(); 
КонецПроцедуры

&НаКлиенте
Процедура ЗначенияДополнительногоСведенияНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка) 
	Если Не Элементы.ЗначенияДополнительногоСведения.ВыбиратьТип Тогда
		СтандартнаяОбработка = Ложь;   
			
		ПараметрыФормы = Новый Структура;
		ПараметрыФормы.Вставить("РежимВыбора", Истина);
		ПараметрыФормы.Вставить("МножественныйВыбор", Истина);
		ПараметрыФормы.Вставить("Отбор", Новый Структура("Владелец", Объект.ДополнительноеСведениеКолонки));
		ПараметрыФормы.Вставить("ВыделитьСтрокиМассив", ЗначенияДополнительногоСведения.ВыгрузитьЗначения());

		ОткрытьФорму(
			"Справочник.ЗначенияСвойствОбъектов.Форма.ФормаСписка", 
			ПараметрыФормы, 
			ЭтаФорма,
			,
			,
			, 
			Новый ОписаниеОповещения("ВыбратьРезультат", ЭтаФорма), 
			РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);   
	КонецЕсли;
КонецПроцедуры
                                 
&НаКлиенте 
Процедура ВыбратьРезультат(МассивРезультатов, ДополнительныеПараметры) Экспорт  
	Если ЗначениеЗаполнено(МассивРезультатов) Тогда  
		ЗначенияДополнительногоСведения.ЗагрузитьЗначения(МассивРезультатов);
	КонецЕсли;
КонецПроцедуры

Функция ПараметрыЗагрузкиНоменклатуры() Экспорт
	
	ПараметрыЗагрузкиНоменклатуры = Новый Структура();
	ПараметрыЗагрузкиНоменклатуры.Вставить("Заголовок", НСтр("ru = 'Загрузка товаров из внешних файлов'"));
	ПараметрыЗагрузкиНоменклатуры.Вставить("ДополнительноеСведение", ""); 
	ПараметрыЗагрузкиНоменклатуры.Вставить("ДатаДокумента", '00010101');
	ПараметрыЗагрузкиНоменклатуры.Вставить("ВидЦены", "");
	
	ПараметрыЗагрузкиНоменклатуры.Вставить("ПараметрыОтбора", Новый Структура);
	
	Возврат ПараметрыЗагрузкиНоменклатуры;
	
КонецФункции

&НаКлиенте
Процедура ЗагрузитьИзВнешнегоФайла(Команда)
	
	ПараметрыЗагрузки = ПараметрыЗагрузкиНоменклатуры();
	ПараметрыЗагрузки.ВидЦены = Объект.ВидЦены;               
	ПараметрыЗагрузки.ДатаДокумента = Объект.Дата;
	ПараметрыЗагрузки.ДополнительноеСведение = Объект.ДополнительноеСведениеКолонки;
	
	Оповещение = Новый ОписаниеОповещения("ЗагрузитьИзВнешнегоФайлаЗавершение", ЭтотОбъект);  	
	ОткрытьФорму("Обработка.ЗагрузкаТоваровИзВнешнихФайлов.Форма.ФормаПроверкиЦенников", ПараметрыЗагрузки,
		Оповещение.Модуль, , , , Оповещение);                                     	
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьИзВнешнегоФайлаЗавершение(АдресЗагруженныхДанных, ДополнительныеПараметры) Экспорт
	Объект.Товары.Очистить();	
	
	Если ЗначениеЗаполнено(АдресЗагруженныхДанных) Тогда
		ПолучитьЗагруженныеТоварыИзХранилища(АдресЗагруженныхДанных);
	КонецЕсли;
	
КонецПроцедуры 

Процедура ПолучитьЗагруженныеТоварыИзХранилища(АдресТоваровВХранилище)
	
	//СтруктураДействий = Новый Структура;
	//СтруктураДействий.Вставить("ПроверитьЗаполнитьСклад", ОбработкаТабличнойЧастиКлиентСервер.ПолучитьСтруктуруЗаполненияСкладаВСтрокеТЧ(Объект, СкладГруппа));
	//СтруктураДействий.Вставить("ЗаполнитьСтавкуНДСВозвратнойТары", Объект.ВернутьМногооборотнуюТару);
	//СтруктураДействий.Вставить("ЗаполнитьПризнакБезВозвратнойТары", Объект.ВернутьМногооборотнуюТару);
	//СтруктураДействий.Вставить("ЗаполнитьПризнакОтмененоБезВозвратнойТары", Объект.ВернутьМногооборотнуюТару);
	//СтруктураДействий.Вставить("ЗаполнитьСодержание", ОбработкаТабличнойЧастиКлиентСервер.ПолучитьСтруктуруЗаполненияСодержанияУслугиВСтрокеТЧ(Объект, Ложь));
	//СтруктураДействий.Вставить("ЗаполнитьПризнакВариантОформленияПродажи", Новый Структура("Номенклатура", "ВариантОформленияПродажи"));
	//СтруктураДействий.Вставить("ЗаполнитьПодразделениеВСтрокеТЧ", Новый Структура("Подразделение", Объект.Подразделение));
	//ДобавитьВСтруктуруДействияПриИзмененииКоличестваУпаковок(СтруктураДействий,Объект);
	//
	КэшированныеЗначения = ПакетнаяОбработкаТабличнойЧастиКлиентСервер.ПолучитьСтруктуруКэшируемыеЗначения();
	//
	ТоварыИзХранилища = ПолучитьИзВременногоХранилища(АдресТоваровВХранилище);
	
	Для Каждого СтрокаТоваров Из ТоварыИзХранилища Цикл
		СтрокаТЧТовары = Объект.Товары.Добавить();
		ЗаполнитьЗначенияСвойств(СтрокаТЧТовары, СтрокаТоваров);
	КонецЦикла;
		
КонецПроцедуры

&НаСервере
Функция ВидПроверкиПоПлану()
	Возврат Объект.ВидПроверки = Перечисления.ВидПроверкиЦенников.ПоПлану;
КонецФункции 

&НаСервере
Функция ВидПроверкиПоПереоценке()
	Возврат Объект.ВидПроверки = Перечисления.ВидПроверкиЦенников.ПоПереоценке;
КонецФункции 

&НаСервере
Процедура ЗаполнитьПоПереоценке()

	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	СобытияПечатиЦенниковСрезПоследних.Номенклатура КАК Номенклатура,
		|	СобытияПечатиЦенниковСрезПоследних.Номенклатура.Код КАК Код,
		|	СобытияПечатиЦенниковСрезПоследних.Цена КАК ЦенаТекущая
		|ИЗ
		|	РегистрСведений.СобытияПечатиЦенников.СрезПоследних КАК СобытияПечатиЦенниковСрезПоследних
		|ГДЕ
		|	СобытияПечатиЦенниковСрезПоследних.ВидЦены = &ВидЦены
		|	И СобытияПечатиЦенниковСрезПоследних.Подразделение = &Подразделение
		|	И СобытияПечатиЦенниковСрезПоследних.Период МЕЖДУ &ДатаНачала И &ДатаОкончания
		|	И НЕ (СобытияПечатиЦенниковСрезПоследних.Период, СобытияПечатиЦенниковСрезПоследних.Номенклатура, СобытияПечатиЦенниковСрезПоследних.Цена) В
		|				(ВЫБРАТЬ РАЗЛИЧНЫЕ
		|					СобытияПечатиЦенниковСрезПоследних.Период КАК Период,
		|					СобытияПечатиЦенниковСрезПоследних.Номенклатура КАК Номенклатура,
		|					СобытияПечатиЦенниковСрезПоследних.Цена КАК Цена
		|				ИЗ
		|					РегистрСведений.СобытияПечатиЦенников.СрезПоследних КАК СобытияПечатиЦенниковСрезПоследних
		|						ЛЕВОЕ СОЕДИНЕНИЕ Документ.ПроверкаЦенников.Товары КАК ПроверкаЦенников
		|						ПО
		|							ПроверкаЦенников.Ссылка.Подразделение = СобытияПечатиЦенниковСрезПоследних.Подразделение
		|								И ПроверкаЦенников.Ссылка.ВидЦены = СобытияПечатиЦенниковСрезПоследних.ВидЦены
		|								И ПроверкаЦенников.Номенклатура = СобытияПечатиЦенниковСрезПоследних.Номенклатура
		|								И Не ПроверкаЦенников.ЕстьРасхождения
		|				ГДЕ
		|					СобытияПечатиЦенниковСрезПоследних.ВидЦены = &ВидЦены
		|					И СобытияПечатиЦенниковСрезПоследних.Подразделение = &Подразделение
		|					И СобытияПечатиЦенниковСрезПоследних.Период МЕЖДУ &ДатаНачала И &ДатаОкончания
		|					И ПроверкаЦенников.Ссылка.Дата >= СобытияПечатиЦенниковСрезПоследних.Период
		|					И ПроверкаЦенников.Ссылка.ВидПроверки = ЗНАЧЕНИЕ(Перечисление.ВидПроверкиЦенников.ПоПереоценке))";
	
	Запрос.УстановитьПараметр("ВидЦены", Объект.ВидЦены);
	Запрос.УстановитьПараметр("ДатаНачала", ПериодПечатиЦен.ДатаНачала);
	Запрос.УстановитьПараметр("ДатаОкончания", ПериодПечатиЦен.ДатаОкончания);
	Запрос.УстановитьПараметр("Подразделение", Объект.Подразделение);
	
	РезультатЗапроса = Запрос.Выполнить();

	Объект.Товары.Загрузить(РезультатЗапроса.Выгрузить());
КонецПроцедуры 


&НаКлиенте
Процедура ВидПроверкиПриИзменении(Элемент)
	Если ВидПроверкиПоПлану() Тогда
		Элементы.ТоварыНетЦенника.Заголовок = "Нет ценника";
	Иначе
		Элементы.ТоварыНетЦенника.Заголовок = "Нет актуального ценника";
	КонецЕсли;
КонецПроцедуры

