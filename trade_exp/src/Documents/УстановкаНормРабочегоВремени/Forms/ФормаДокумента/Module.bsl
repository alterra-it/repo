////////////////////// События формы /////////////////////////////
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Не ЗначениеЗаполнено(Объект.ПериодРегистрации) Тогда
		Объект.ПериодРегистрации= НачалоМесяца(ТекущаяДата());
	КонецЕсли;
	МесяцРегистрацииСтрокой= УчетРабочегоВремениСервер.ПолучитьПредставлениеМесяца(Объект.ПериодРегистрации);
	Если Не ЗначениеЗаполнено(Объект.Автор) Тогда
		Объект.Автор= Пользователи.ТекущийПользователь();
	КонецЕсли;
КонецПроцедуры

////////////////////// События элементов /////////////////////////////
&НаКлиенте
Процедура ОтделПриИзменении(Элемент)
	ОтделПриИзмененииСервер();
КонецПроцедуры

&НаКлиенте
Процедура МесяцРегистрацииСтрокойНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	ОповещениеЗавершения = Новый ОписаниеОповещения("ПериодРегистрацииНачалоВыбораЗавершение", ЭтаФорма);
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("Форма", ЭтаФорма);
	ДополнительныеПараметры.Вставить("РедактируемыйОбъект", ЭтаФорма);
	ДополнительныеПараметры.Вставить("ПутьРеквизита", "Объект.ПериодРегистрации");
	ДополнительныеПараметры.Вставить("ПутьРеквизитаПредставления", "МесяцРегистрацииСтрокой");
	ДополнительныеПараметры.Вставить("ИзменитьМодифицированность", Истина);
	ДополнительныеПараметры.Вставить("ОповещениеЗавершения", ОповещениеЗавершения);
	
	Значение = ОбщегоНазначенияКлиентСервер.ПолучитьРеквизитФормыПоПути(ЭтаФорма, "Объект.ПериодРегистрации");
	
	ОткрытьФорму("ОбщаяФорма.ВыборПериода", 
	Новый Структура("Значение,РежимВыбораПериода,ЗапрашиватьРежимВыбораПериодаУВладельца", Значение, "Месяц", Ложь),
	ЭтаФорма, , , ,ОповещениеЗавершения , РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура ПериодРегистрацииНачалоВыбораЗавершение(ЗначениеВыбрано, ДополнительныеПараметры) Экспорт
	Объект.ПериодРегистрации= ЗначениеВыбрано;
	МесяцРегистрацииСтрокой= УчетРабочегоВремениКлиент.ПолучитьПредставлениеМесяца(Объект.ПериодРегистрации);
КонецПроцедуры

&НаКлиенте
Процедура МесяцРегистрацииСтрокойРегулирование(Элемент, Направление, СтандартнаяОбработка)
	Объект.ПериодРегистрации = ДобавитьМесяц(Объект.ПериодРегистрации, Направление);
	МесяцРегистрацииСтрокой= УчетРабочегоВремениКлиент.ПолучитьПредставлениеМесяца(Объект.ПериодРегистрации);
КонецПроцедуры


////////////////////// Команды /////////////////////////////
&НаКлиенте
Процедура Рассчитать(Команда)
	Если Объект.НормыВремени.Количество() < 7 Тогда
		Сообщить("Необходимо предворительно заполнить нормы по всем дням недели!");
		Возврат;
	КонецЕсли;
	
	РасчитатьКлиент();
КонецПроцедуры

&НаКлиенте
Процедура ЗаполнитьПоУмолчанию(Команда)
	ДиалогСВопросомОчиститьДанные();
КонецПроцедуры

////////////////////// Вспомогательные функции /////////////////////////////
&НаКлиенте
Процедура ЗаполнитьПоУмолчаниюКлиент()
	Объект.НормыВремени.Очистить();
	мРасписание= УчетРабочегоВремениСервер.ПолучитьЧасыработыОрганизации(Объект.ГрафикРаботы);
	Для ДН=1 По 7 Цикл
		НоваяСтрока= Объект.НормыВремени.Добавить();
		НоваяСтрока.ДеньНедели= ДН;
		НоваяСтрока.ДеньНеделиСтрокой= АльтерраОбщегоНазначения.ДеньНеделиПрописьюСокр(НоваяСтрока.ДеньНедели);
		НоваяСтрока.ЧасыРаботыОрганизацииВДень= мРасписание[ДН-1];
		НоваяСтрока.КоличествоЧеловек= Объект.КоличествоЧеловекВОтделе;
	КонецЦикла;
КонецПроцедуры

&НаКлиенте
Процедура РасчитатьКлиент()
	КолБудней= УчетРабочегоВремениСервер.ПолучитьКоличествоДнейВМесяц(Объект.ПериодРегистрации, КонецМесяца(Объект.ПериодРегистрации), ПредопределенноеЗначение("Перечисление.ВидыДнейПроизводственногоКалендаря.Рабочий"));
	КолСуббот= УчетРабочегоВремениСервер.ПолучитьКоличествоДнейВМесяц(Объект.ПериодРегистрации, КонецМесяца(Объект.ПериодРегистрации), ПредопределенноеЗначение("Перечисление.ВидыДнейПроизводственногоКалендаря.Суббота"));
	КолВоскресений= УчетРабочегоВремениСервер.ПолучитьКоличествоДнейВМесяц(Объект.ПериодРегистрации, КонецМесяца(Объект.ПериодРегистрации), ПредопределенноеЗначение("Перечисление.ВидыДнейПроизводственногоКалендаря.Воскресенье"));
	КолПраздников= УчетРабочегоВремениСервер.ПолучитьКоличествоДнейВМесяц(Объект.ПериодРегистрации, КонецМесяца(Объект.ПериодРегистрации), ПредопределенноеЗначение("Перечисление.ВидыДнейПроизводственногоКалендаря.Предпраздничный"));
	
	РобочиеЧасыВДеньСБ= Объект.НормыВремени[6-1].ЧасыРаботыОрганизацииВДень;
	РобочиеЧасыВДеньВС= Объект.НормыВремени[7-1].ЧасыРаботыОрганизацииВДень;
	КоличествоЧеловекСБ= Объект.НормыВремени[6-1].КоличествоЧеловек;
	КоличествоЧеловекВС= Объект.НормыВремени[7-1].КоличествоЧеловек;
	КоличествоЧеловекБудни= Объект.НормыВремени.Итог("КоличествоЧеловек")-КоличествоЧеловекСБ-КоличествоЧеловекВС;
	
	
	Для ДН=1 По 7 Цикл
		ТекущаяСтрока= Объект.НормыВремени[ДН-1];
		Если ТекущаяСтрока.ДеньНедели < 6 Тогда
			НормаЧасовРаботыНаЧеловекаВДень= (КолБудней*Объект.НормаЧасовВМесяцНаСотрудника-(КолСуббот*РобочиеЧасыВДеньСБ*КоличествоЧеловекСБ+КолВоскресений*РобочиеЧасыВДеньВС*КоличествоЧеловекВС))/КоличествоЧеловекБудни/КолБудней;
			ТекущаяСтрока.НормаЧасовРаботыНаЧеловекаВДень= НормаЧасовРаботыНаЧеловекаВДень;
		ИначеЕсли ТекущаяСтрока.ДеньНедели = 6 Тогда
			ТекущаяСтрока.НормаЧасовРаботыНаЧеловекаВДень= ТекущаяСтрока.ЧасыРаботыОрганизацииВДень;
		ИначеЕсли ТекущаяСтрока.ДеньНедели = 7 Тогда
			ТекущаяСтрока.НормаЧасовРаботыНаЧеловекаВДень= ТекущаяСтрока.ЧасыРаботыОрганизацииВДень;
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

&НаКлиенте
Процедура ДиалогСВопросомОчиститьДанные()
	Оповещение = Новый ОписаниеОповещения("ПослеЗакрытияВопроса",
	ЭтаФорма);	
	
	ПоказатьВопрос(Оповещение,
	"Очистить введенные данные?",
	РежимДиалогаВопрос.ДаНетОтмена,
	0, // таймаут в секундах
	КодВозвратаДиалога.Да, // (необ.) кнопка по умолчанию
	"Очистить?" // (необ.) заголовок
	);
КонецПроцедуры

&НаКлиенте
Процедура ПослеЗакрытияВопроса(Результат, Параметры) Экспорт		
	Если Результат = КодВозвратаДиалога.Да Тогда
		ЗаполнитьПоУмолчаниюКлиент();
	КонецЕсли;	
КонецПроцедуры

&НаСервере
Процедура ОтделПриИзмененииСервер()
Объект.КоличествоЧеловекВОтделе= УчетРабочегоВремениСервер.ПолучитьКоличествоСотрудниковВПодразделении(Объект.Подразделение);
Объект.ГрафикРаботы= ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Объект.Отдел, "ГрафикРаботы");
КонецПроцедуры
