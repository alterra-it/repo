#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
&После("ОбработкаЗаполнения")
Процедура Док_ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка)
	Если ТипЗнч(ДанныеЗаполнения) = Тип("ДокументСсылка.ПорчаТоваров") Тогда
		
		Склад = ДанныеЗаполнения.Склад;
		
		Для каждого Строка из ДанныеЗаполнения.Товары Цикл
			НоваяСтрока = Товары.Добавить();
			ЗаполнитьЗначенияСвойств(НоваяСтрока,Строка);
			НоваяСтрока.КоличествоУпаковок = Строка.Количество;
		КонецЦикла;

	КонецЕсли;
КонецПроцедуры


&После("ОбработкаПроведения")
Процедура Док_ОбработкаПроведения(Отказ, РежимПроведения)
	
	//Альтерра
	ДопМодуль.КонтрольОстатков(ЭтотОбъект,Отказ);
    //-Альтерра        
	
	//Альтерра
	Если Ссылка.ПодтвержденСкладом Тогда
		РегистрыСведений.РазмещениеНоменклатурыПоСкладскимЯчейкам.СоздатьЯчейки(Отказ, Товары.ВыгрузитьКолонку("Номенклатура"), Склад, РабочийУчасток);
		РегистрыСведений.РазмещениеНоменклатурыПоСкладскимЯчейкам.СоздатьЯчейки(Отказ, Товары.ВыгрузитьКолонку("НоменклатураОприходование"), Склад, РабочийУчасток);
    КонецЕсли;
    //-Альтерра 
КонецПроцедуры

#КонецЕсли
