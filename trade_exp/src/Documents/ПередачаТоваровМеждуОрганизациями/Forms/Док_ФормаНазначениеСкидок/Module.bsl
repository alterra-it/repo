
&НаКлиенте
Процедура Перенести(Команда)
СтруктураВыбора = Новый Структура();
СтруктураВыбора.Вставить("ВидЦены",ВидЦены);
СтруктураВыбора.Вставить("ПроцентСкидки",процентСкидки);
СтруктураВыбора.Вставить("ЦенаПокупки",ЦенаПокупки);
СтруктураВыбора.Вставить("ПроцентНадбавки",ПроцентНадбавки);

Закрыть(СтруктураВыбора);
КонецПроцедуры

&НаКлиенте
Процедура ЗакрытьОкно(Команда)
	 Закрыть();
КонецПроцедуры
