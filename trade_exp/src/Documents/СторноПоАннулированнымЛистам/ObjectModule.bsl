#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
&Вместо("ОбработкаПроведения")
Процедура ОбработкаПроведения(Отказ, Режим)

	// регистр НачисленияУдержаний 
	Движения.НачисленияУдержаний.Записывать = Истина;
	Для Каждого ТекСтрокаНачисления Из Начисления Цикл
		Движение = Движения.НачисленияУдержаний.Добавить();
		Движение.Период = Дата;
		Движение.ПериодНачисления = ПериодНачисления;
		Движение.Сотрудник = ТекСтрокаНачисления.Сотрудник;
		Движение.Удержание = ТекСтрокаНачисления.Удержание;
		Движение.СуммаСторно = ТекСтрокаНачисления.Сторно;
		Движение.РегистраторУдержания= ТекСтрокаНачисления.РегистраторУдержания;
	КонецЦикла;
КонецПроцедуры

#КонецЕсли