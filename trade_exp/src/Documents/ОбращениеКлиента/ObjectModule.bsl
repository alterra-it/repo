#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

&Вместо("ОбработкаПроверкиЗаполнения")
Процедура Док_ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
	УслугаРазгрузки = АльтерраОбщегоНазначения.УслугаРазгрузкиСНДС();
	Для ТекИндекс = 0 По ТоварыДляДовоза.Количество()-1 Цикл
		
		СтрокаТовары = ТоварыДляДовоза[ТекИндекс]; // СтрокаТабличнойЧасти
		
		Если Не ЗначениеЗаполнено(СтрокаТовары.Количество) Тогда
			
			Если СтрокаТовары.Номенклатура = УслугаРазгрузки Тогда
				ТекстОшибки = НСтр("ru='Не заполнено поле ""Количество излишка""в строке %НомерСтроки% списка ""Товары для довоза""'");
				ТекстОшибки =  СтрЗаменить(ТекстОшибки, "%НомерСтроки%", СтрокаТовары.НомерСтроки);
				
				ОбщегоНазначения.СообщитьПользователю(
					ТекстОшибки,
					ЭтотОбъект,
					ОбщегоНазначенияКлиентСервер.ПутьКТабличнойЧасти("ТоварыДляДовоза", СтрокаТовары.НомерСтроки, "КоличествоИзлишка"),
					,
					Отказ);
			Иначе
	            ТекстОшибки = НСтр("ru='Не заполнено ни одно из полей: поле ""Количество недостачи"" или поле ""Количество излишка""в строке %НомерСтроки% списка ""Товары для довоза""'");
				ТекстОшибки =  СтрЗаменить(ТекстОшибки, "%НомерСтроки%", СтрокаТовары.НомерСтроки);
				
				ОбщегоНазначения.СообщитьПользователю(
					ТекстОшибки,
					ЭтотОбъект,
					ОбщегоНазначенияКлиентСервер.ПутьКТабличнойЧасти("ТоварыДляДовоза", СтрокаТовары.НомерСтроки, "КоличествоНедостачи"),
					,
					Отказ); 
					
				ОбщегоНазначения.СообщитьПользователю(
					ТекстОшибки,
					ЭтотОбъект,
					ОбщегоНазначенияКлиентСервер.ПутьКТабличнойЧасти("ТоварыДляДовоза", СтрокаТовары.НомерСтроки, "КоличествоИзлишка"),
					,
					Отказ);
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЦикла;	
КонецПроцедуры
	
&Вместо("ОбработкаЗаполнения")
Процедура ОбработкаЗаполнения(ДанныеЗаполнения, СтандартнаяОбработка)
	
	Статус = Перечисления.СтатусыОбращенийКлиентов.ЖдетРешения;
	Если ДанныеЗаполнения<>Неопределено Тогда
		Основание = ДанныеЗаполнения;
		Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ДанныеЗаполнения, "Партнер") Тогда
			Клиент = ДанныеЗаполнения.Партнер;
		КонецЕсли;
		Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ДанныеЗаполнения,"Подразделение") Тогда
			Если ТипЗнч(ДанныеЗаполнения) = Тип("СправочникСсылка.Претензии") Тогда
				Подразделение = ДанныеЗаполнения.ВиновноеПодразделение;
			Иначе
				Подразделение = ДанныеЗаполнения.Подразделение;
			КонецЕсли;
		КонецЕсли;
		//Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ДанныеЗаполнения, "АдресДоставки") Тогда
		Если ТипЗнч(ДанныеЗаполнения)=Тип("ДокументСсылка.ЗаказНаДоставкуКлиенту") Тогда
			АдресДоставки = ДанныеЗаполнения.АдресДоставки;
		КонецЕсли;
		
		Если ТипЗнч(ДанныеЗаполнения) = Тип("СправочникСсылка.МаркетинговыеМероприятия") Тогда
			МаркетинговоеМероприятие = ДанныеЗаполнения.Ссылка;
		ИначеЕсли ТипЗнч(ДанныеЗаполнения) = Тип("СправочникСсылка.Претензии") Тогда
			Содержание = ДанныеЗаполнения.ОписаниеПретензии;
			ОтветственныйЗаРешение = ДанныеЗаполнения.Ответственный;
			Результат = ДанныеЗаполнения.РезультатыОтработки;
		КонецЕсли;
		
		Если ТипЗнч(ДанныеЗаполнения)=Тип("ДокументСсылка.ВозвратТоваровОтКлиента") Или ТипЗнч(ДанныеЗаполнения)=Тип("ДокументСсылка.ЗаказНаДоставкуКлиенту") Или
			ТипЗнч(ДанныеЗаполнения)=Тип("ДокументСсылка.ЗаказКлиента") Или ТипЗнч(ДанныеЗаполнения)=Тип("ДокументСсылка.РеализацияТоваровУслуг") Или ТипЗнч(ДанныеЗаполнения)=Тип("ДокументСсылка.ЧекККМ")  Тогда
			ЗаполнитьСвязаннымиДокументами(ДанныеЗаполнения);	
		КонецЕсли;
	КонецЕсли;
КонецПроцедуры

Функция ПолучитьВодителя(ЗаказНаДоставку) Экспорт
	Запрос = Новый Запрос();
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ЗаданиеНаПеревозкуРейсОснования.Ссылка.Водитель КАК Водитель,
	|	ЗаданиеНаПеревозкуРейсОснования.Ссылка.Перевозчик КАК Перевозчик
	|ИЗ
	|	Документ.ЗаданиеНаПеревозкуРейс.Основания КАК ЗаданиеНаПеревозкуРейсОснования
	|ГДЕ
	|	ЗаданиеНаПеревозкуРейсОснования.Документ = &Ссылка
	|	И ЗаданиеНаПеревозкуРейсОснования.Ссылка.ПометкаУдаления = ЛОЖЬ
	|	И ЗаданиеНаПеревозкуРейсОснования.Ссылка.Проведен = ИСТИНА";
	Запрос.УстановитьПараметр("Ссылка", ЗаказНаДоставку);
	РезультатЗапроса = Запрос.Выполнить();
	Выборка = РезультатЗапроса.Выбрать();
	Если Выборка.Следующий() Тогда
		Возврат Новый Структура("Водитель, Перевозчик", Выборка.Водитель, Выборка.Перевозчик);
	КонецЕсли;
	Возврат Неопределено;
КонецФункции // ПолучитьВодителя()

Процедура ЗаполнитьСвязаннымиДокументами(Документ) Экспорт
	//документ продажи
	Если ТипЗнч(Документ)=Тип("ДокументСсылка.ЧекККМ") ИЛИ ТипЗнч(Документ)=Тип("ДокументСсылка.РеализацияТоваровУслуг") Тогда
		ДокументПродажи = Документ;
	ИначеЕсли  ТипЗнч(Документ)=Тип("ДокументСсылка.ВозвратТоваровОтКлиента") Тогда
		Если ТипЗнч(Документ.ДокументРеализации) = Тип("ДокументСсылка.ОтчетОРозничныхПродажах") Тогда
			ДокументПродажи = Документ.ЧекККМ;
		Иначе
			ДокументПродажи = Документ.ДокументРеализации;
		КонецЕсли;
	ИначеЕсли ТипЗнч(Документ)=Тип("ДокументСсылка.ЗаказНаДоставкуКлиенту") Тогда
		Если Документ.Основания.Количество() = 1 Тогда 
			ДокументЗаказаНаДоставку = Документ.Основания[0].Документ;
			Если ТипЗнч(ДокументЗаказаНаДоставку)=Тип("ДокументСсылка.ЧекККМ") ИЛИ ТипЗнч(ДокументЗаказаНаДоставку)=Тип("ДокументСсылка.РеализацияТоваровУслуг") Тогда
				ДокументПродажи = ДокументЗаказаНаДоставку;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Если ТипЗнч(Документ)=Тип("ДокументСсылка.ЗаказНаДоставкуКлиенту") Тогда
		ЗаказНаДоставку = Документ;
	Иначе
		ЗаказНаДоставку = ПолучитьДокументДоставки(Документ, ДокументПродажи); 
	КонецЕсли;
	
	Если ЗаказНаДоставку <> Документы.ЗаказНаДоставкуКлиенту.ПустаяСсылка() Тогда
		//i по доставке
		СтрукутраВодительПеревозчик = ПолучитьВодителя(ЗаказНаДоставку);
		Если ЗначениеЗаполнено(СтрукутраВодительПеревозчик) Тогда
			Водитель = СтрукутраВодительПеревозчик.Водитель;
			Перевозчик = СтрукутраВодительПеревозчик.Перевозчик;
		КонецЕсли;
		ПодразделениеОтгрузки = ЗаказНаДоставку.Подразделение;
		АдресДоставки = ЗаказНаДоставку.АдресДоставки; 
		ТелефонКлиентаНаВыгрузке = ЗаказНаДоставку.ТелефонКлиентаНаВыгрузке;
	КонецЕсли;
	
	Если ТипЗнч(Документ)=Тип("ДокументСсылка.РеализацияТоваровУслуг") Тогда
		ТелефонКлиентаНаВыгрузке = Документ.ТелефонКлиента;
	ИначеЕсли ТипЗнч(Документ)=Тип("ДокументСсылка.ЧекККМ") Тогда
		Если ЗаказНаДоставку = Документы.ЗаказНаДоставкуКлиенту.ПустаяСсылка() Тогда
			Водитель = Документ.Водитель;
			ТелефонКлиентаНаВыгрузке = Документ.ТелефонКлиентаНаВыгрузке;
		КонецЕсли;
	КонецЕсли;

КонецПроцедуры

Функция ПолучитьДокументДоставки(Документ, Продажа)
	ОснованияЗаказаНаДоставку = Новый Массив;
	ОснованияЗаказаНаДоставку.Добавить(Документ); 
	
	Если ЗначениеЗаполнено(Продажа) Тогда
		ОснованияЗаказаНаДоставку.Добавить(Продажа);
		Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Продажа, "ЗаказКлиента") Тогда
			ОснованияЗаказаНаДоставку.Добавить(Продажа.ЗаказКлиента);
		КонецЕсли;
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	ЗаказНаДоставкуКлиентуОснования.Ссылка КАК Ссылка
		|ИЗ
		|	Документ.ЗаказНаДоставкуКлиенту.Основания КАК ЗаказНаДоставкуКлиентуОснования
		|ГДЕ
		|	ЗаказНаДоставкуКлиентуОснования.Документ В(&Документы)
		|	И ЗаказНаДоставкуКлиентуОснования.Ссылка.ПометкаУдаления = ЛОЖЬ
		|	И ЗаказНаДоставкуКлиентуОснования.Ссылка.Проведен = ИСТИНА";
	Запрос.УстановитьПараметр("Документы", ОснованияЗаказаНаДоставку);
	РезультатЗапроса = Запрос.Выполнить();
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	Если ВыборкаДетальныеЗаписи.Следующий() Тогда
		Возврат ВыборкаДетальныеЗаписи.Ссылка;
	КонецЕсли;
	Возврат Неопределено;
	
КонецФункции // ПолучитьСвязанныеДокументыКлиента()

&Вместо("ПередЗаписью")
Процедура Док_ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
		
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	ОбновлениеИнформационнойБазы.ПроверитьОбъектОбработан(ЭтотОбъект);         
	
	ПроведениеДокументов.ПередЗаписьюДокумента(ЭтотОбъект, РежимЗаписи, РежимПроведения);
	
	ЭтоНовыйДокумент = ЭтоНовый();
	ДополнительныеСвойства.Вставить("ЭтоНовый",    ЭтоНовыйДокумент);
	ДополнительныеСвойства.Вставить("РежимЗаписи", РежимЗаписи);
	Если ЭтоНовыйДокумент И НЕ ЗначениеЗаполнено(Номер) Тогда
		УстановитьНовыйНомер();
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Автор) И ЭтоНовый() Тогда
		Автор = Пользователи.ТекущийПользователь();
	КонецЕсли;
	
	Если (РежимЗаписи = РежимЗаписиДокумента.ОтменаПроведения Или ЭтотОбъект.ПометкаУдаления = Истина) И ЭтотОбъект.ПолучитьЗаказНаДовоз() <> Неопределено Тогда
		 Сообщить("По обращению клиента существует активный заказ на довоз");
		Если НЕ РольДоступна("ПолныеПрава") Тогда
		 	Отказ = Истина;
		КонецЕсли;
	КонецЕсли	
	
КонецПроцедуры

&Вместо("ПриЗаписи")
Процедура Док_ПриЗаписи(Отказ)
		Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	ПроведениеДокументов.ПриЗаписиДокумента(ЭтотОбъект, Отказ);
КонецПроцедуры

&Вместо("ОбработкаПроведения")
Процедура Док_ОбработкаПроведения(Отказ, РежимПроведения)
	
	ПроведениеДокументов.ОбработкаПроведенияДокумента(ЭтотОбъект, Отказ);
			
КонецПроцедуры

//Процедура ОбработкаУдаления(Отказ)

Функция ПолучитьЗаказНаДовоз() Экспорт
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	ЗаказНаДоставкуКлиенту.Ссылка КАК Ссылка
		|ИЗ
		|	Документ.ЗаказНаДоставкуКлиенту.Основания КАК ЗаказНаДоставкуКлиентуОснования
		|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ЗаказНаДоставкуКлиенту КАК ЗаказНаДоставкуКлиенту
		|		ПО ЗаказНаДоставкуКлиентуОснования.Ссылка = ЗаказНаДоставкуКлиенту.Ссылка
		|ГДЕ
		|	ЗаказНаДоставкуКлиентуОснования.Документ = &Документ
		|	И ЗаказНаДоставкуКлиенту.ПометкаУдаления = ЛОЖЬ
		|	И ЗаказНаДоставкуКлиенту.Проведен = ИСТИНА";    
	
	
	Запрос.УстановитьПараметр("Документ", Ссылка);	
	РезультатЗапроса = Запрос.Выполнить();
	Выборка = РезультатЗапроса.Выбрать();
	
	Если Выборка.Следующий() Тогда
		Возврат Выборка.Ссылка;
	Иначе
		Возврат Неопределено;
	КонецЕсли; 
	
КонецФункции

#КонецЕсли



