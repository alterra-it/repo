
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

&После ("ДобавитьКомандыСозданияНаОсновании")
Процедура Док_ДобавитьКомандыСозданияНаОсновании(КомандыСозданияНаОсновании, Параметры) Экспорт
		
	Документы.ОрдерНаОтражениеИзлишковТоваров.ДобавитьКомандуСоздатьНаОсновании(КомандыСозданияНаОсновании);
	
	Документы.ОрдерНаОтражениеНедостачТоваров.ДобавитьКомандуСоздатьНаОсновании(КомандыСозданияНаОсновании);
	
КонецПроцедуры

Процедура ОтправитьИнформациюВЧат_ИзменениеПриходныхДокументовВЗакрытомПериоде(Организация, Дата, ТелоСообщения) Экспорт
	
	Если Организация <> АльтерраОбщегоНазначения.ПолучитьАСКР() И АльтерраОбщегоНазначения.ЭтоРабочаяБаза() Тогда
		
		ДатаЗакрытияПериода = Справочники.ТипПериод.ДатаЗакрытияПериода; 
		
		Если Дата >= ДатаЗакрытияПериода.НачалоПериода И Дата <= ДатаЗакрытияПериода.КонецПериода Тогда
        	АльтерраВзаимодействиеСБитрикс24.ОтправитьСообщениеВБитриксПоИДЧата(
				ТелоСообщения, 
				"chat126253");
		КонецЕсли;
	КонецЕсли;     

КонецПроцедуры

Процедура ОбъектМодифицирован (СтарыйОбъект, НовыйОбъект, РеквизитыДляПроверки, ТЧДляПроверки, ДополнительныеСвойства) Экспорт
	
	СтрДляОтправкиСообщения = "";
	Для Каждого Элемент Из РеквизитыДляПроверки Цикл 
		Если СтарыйОбъект[Элемент.Реквизит] <> НовыйОбъект[Элемент.Реквизит] Тогда
			СтрДляОтправкиСообщения = СтрДляОтправкиСообщения + ?(ЗначениеЗаполнено(СтрДляОтправкиСообщения), ", ", "") + Элемент.ИмяРеквизита;
		КонецЕсли;
	КонецЦикла;    

	Для Каждого Элемент Из ТЧДляПроверки Цикл
		Если ОбщегоНазначения.КонтрольнаяСуммаСтрокой(СтарыйОбъект[Элемент.ТЧ].Выгрузить()) <> 
			ОбщегоНазначения.КонтрольнаяСуммаСтрокой(НовыйОбъект[Элемент.ТЧ].Выгрузить()) Тогда
			
			СтрДляОтправкиСообщения = СтрДляОтправкиСообщения + ?(ЗначениеЗаполнено(СтрДляОтправкиСообщения), ", ", "") + Элемент.ИмяТЧ;
		КонецЕсли;
	КонецЦикла;
	
	Если ЗначениеЗаполнено(СтрДляОтправкиСообщения) Тогда
		ДополнительныеСвойства.Вставить(
			"ОтправитьИнформациюВЧат_ИзменениеПриходныхДокументовВЗакрытомПериоде", 
			"Изменен документ " + НовыйОбъект.Ссылка + ". Внесены изменения в: " + СтрДляОтправкиСообщения);
	КонецЕсли;     

КонецПроцедуры

#КонецЕсли
