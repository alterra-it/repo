
&НаСервере
Процедура Док_ПриСозданииНаСервереПосле(Отказ, СтандартнаяОбработка)
	//Идентификатор = "e0769809-4357-4d04-abb3-dcabc7617bef";
	//АдресВХ = ПоместитьВоВременноеХранилище(Идентификатор, Новый УникальныйИдентификатор());
КонецПроцедуры

&НаСервере
Процедура Док_ОбработкаПроверкиЗаполненияНаСервереПеред(Отказ, ПроверяемыеРеквизиты)
	ПроверяемыеРеквизиты.Добавить("БезопасныйРежим");
КонецПроцедуры

&НаКлиенте
Процедура Док_ПараметрыИОтборыПередНачаломИзмененияПосле(Элемент, Отказ)
	//Элементы.ПараметрыИОтборыПериодПараметра.Доступность = Истина;
	//Отказ = Ложь;
	//СтрокаТЧ=Элементы.ПараметрыИОтборы.ТекущиеДанные;
	//Если   НЕ (СтрокаТЧ.ЭтоПараметр И СтрокаТЧ.Наименование = "Период") Тогда
		//Элементы.ПараметрыИОтборыПериодПараметра.Доступность = Ложь;
		//Отказ = Истина;
	//КонецЕсли;
КонецПроцедуры

//+Альтерра
&НаСервере
&После("УстановитьВидимостьДоступность")
Процедура Док_УстановитьВидимостьДоступность(Регистрация)
	Элементы.СтраницаОтборы.Доступность = РольДоступна("ПолныеПрава");
КонецПроцедуры
//-
