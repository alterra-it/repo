
//&НаКлиенте
//Процедура ИнтервалыИнтервалНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
// СтандартнаяОбработка = Ложь;	
// ЗначениеОтбора  = Новый Структура("ПометкаУдаления", Ложь);	
// ПараметрыФормы = Новый Структура("МножественныйВыбор, Отбор", Истина, ЗначениеОтбора);	
// ОткрытьФорму("Справочник.ИнтервалыДоставки.ФормаВыбора", ПараметрыФормы, Элементы.Интервалы);
//	
//КонецПроцедуры

&НаКлиенте
Процедура Подбор(Команда)
 ЗначениеОтбора  = Новый Структура("ПометкаУдаления", Ложь);	
 ПараметрыФормы = Новый Структура("МножественныйВыбор, Отбор", Истина, ЗначениеОтбора);	
 ОткрытьФорму("Справочник.ИнтервалыДоставки.ФормаВыбора", ПараметрыФормы, Элементы.Интервалы);	
КонецПроцедуры

&НаКлиенте
Процедура ИнтервалыОбработкаВыбора(Элемент, ВыбранноеЗначение, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;	
	Для Каждого ВыбранныйЭлемент Из ВыбранноеЗначение Цикл 
		НоваяСтрока = Объект.Интервалы.Добавить();
		НоваяСтрока.Интервал = ВыбранныйЭлемент; 
	КонецЦикла;
КонецПроцедуры

