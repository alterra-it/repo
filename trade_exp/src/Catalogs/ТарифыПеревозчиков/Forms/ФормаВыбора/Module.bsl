
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Параметры.Свойство("ВидДоставки") И ЗначениеЗаполнено(Параметры.ВидДоставки) Тогда
		ОтборПоВидуДоставки = Параметры.ВидДоставки;
		ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(Список, "ВидДоставки", ОтборПоВидуДоставки, ВидСравненияКомпоновкиДанных.Равно,,Истина);
	КонецЕсли;
	
	Если Параметры.Свойство("ТипТС") И ЗначениеЗаполнено(Параметры.ТипТС) Тогда
		ОтборПоТипуТС = Параметры.ТипТС;
		ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(Список, "ТипТС", ОтборПоТипуТС, ВидСравненияКомпоновкиДанных.Равно,,Истина);
	КонецЕсли;
	
	Если Параметры.Свойство("ВыделитьСтрокиМассив") Тогда
		МассивСтрок = Параметры.ВыделитьСтрокиМассив;
		Если ЗначениеЗаполнено(МассивСтрок) Тогда
			ВыделитьСтроки(МассивСтрок);
		КонецЕсли;
	КонецЕсли;
КонецПроцедуры

&НаСервере
Процедура ВыделитьСтроки(МассивСсылок)
    ВыделенныеСтроки = Элементы.Список.ВыделенныеСтроки;
    ВыделенныеСтроки.Очистить();
	Элементы.Список.ТекущаяСтрока = МассивСсылок[0];
    Для Каждого НайденнаяСсылка Из МассивСсылок Цикл
        ВыделенныеСтроки.Добавить(НайденнаяСсылка);
	КонецЦикла;		
КонецПроцедуры

&НаКлиенте
Процедура ОтборПоТипуТСПриИзменении(Элемент)
	ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбора(Список.Отбор, "ТипТС", ОтборПоТипуТС, ВидСравненияКомпоновкиДанных.Равно, , ЗначениеЗаполнено(ОтборПоТипуТС));
КонецПроцедуры

&НаКлиенте
Процедура ОтборПоВидуДоставкиПриИзменении(Элемент)
	ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбора(Список.Отбор, "ВидДоставки", ОтборПоВидуДоставки, ВидСравненияКомпоновкиДанных.Равно, , ЗначениеЗаполнено(ОтборПоВидуДоставки));
КонецПроцедуры
