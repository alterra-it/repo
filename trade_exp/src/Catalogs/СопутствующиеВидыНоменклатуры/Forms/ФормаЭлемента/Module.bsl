
&НаКлиенте
Процедура ВидНоменклатурыПриИзменении(Элемент)
ИзменитьНаименованиеНоменклатуры();	
КонецПроцедуры

&НаКлиенте
Процедура СопутствующийВидНоменклатурыПриИзменении(Элемент)
ИзменитьНаименованиеНоменклатуры();	
КонецПроцедуры

&НаКлиенте
Процедура ИзменитьНаименованиеНоменклатуры()
	
	НаименованиеВидаНоменклатуры = "";
	НаименованиеСопутствующегоВидНоменклатуры = "";
	
	Если Не Объект.Владелец.Пустая() Тогда
		НаименованиеВидаНоменклатуры = Строка(Объект.Владелец);		
	КонецЕсли;
	
	Если Не Объект.СопутствующийВидНоменклатуры.Пустая() Тогда
		НаименованиеСопутствующегоВидНоменклатуры = Строка(Объект.СопутствующийВидНоменклатуры);	
	КонецЕсли;
	
	Объект.Наименование = НаименованиеВидаНоменклатуры + "_" +НаименованиеСопутствующегоВидНоменклатуры;
	
	ЭтаФорма.ОбновитьОтображениеДанных();
	
КонецПроцедуры
