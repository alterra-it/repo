
&НаСервере
Процедура Док_ПриСозданииНаСервереПосле(Отказ, СтандартнаяОбработка)
	Элементы.ОграничениеВыбораВидаЦены.ТолькоПросмотр = не (рольДоступна("ПолныеПрава") или рольДоступна("НазначениеСтатусовКлиентовВБонуснойПрограмме"));
	//Элементы.ГруппаВидовЦен.Видимость=ДопМодуль.НовоеЦенообразование();
	//Элементы.ФорматПодразделения.Видимость=ДопМодуль.НовоеЦенообразование();
	//Элементы.РегионПрисутствия.Видимость=ДопМодуль.НовоеЦенообразование();  
	//Элементы.СтраницаУточнениеПоЦеновымГруппам_МД.Видимость=ДопМодуль.НовоеЦенообразование();
КонецПроцедуры

#Область ЗагрузкаИзФайла
&НаКлиенте
Процедура ЗагрузитьИзФайла_Альтерра(Команда)
	ОписаниеОповещения = Новый ОписаниеОповещения("ПослеЗагрузкиИзФайла", ЭтотОбъект);
	ПараметрыФормы= Новый Структура();
	ПараметрыФормы.Вставить("НастройкиТаблицы",СтруктураТаблицы());
	ОткрытьФорму("Обработка.ЗагрузкаИзВнешнегоФайла_Альтерра.Форма.ФормаПроизвольная", ПараметрыФормы, ЭтаФорма,,,, ОписаниеОповещения, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
КонецПроцедуры

&НаКлиенте
Функция СтруктураТаблицы()
	
	СтруктураТаблицы=Новый Структура();
	
	СтруктураТаблицы.Вставить("Код","строка#Код#15#1#Справочники.Номенклатура.НайтиПоКоду(""&Ячейка&"")#Номенклатура");
	СтруктураТаблицы.Вставить("Номенклатура","СправочникСсылка.Номенклатура#Номенклатура#30#1#Справочники.Номенклатура.НайтиПоНаименованию(""&Ячейка&"")#Номенклатура");
	СтруктураТаблицы.Вставить("Наценка","число#Наценка, %#10");

	возврат  СтруктураТаблицы;
КонецФункции 

&НаКлиенте
Процедура ПослеЗагрузкиИзФайла(Результат, ДополнительныеПараметры) Экспорт
	
	ПослеЗагрузкиИзФайлаСервер(Результат, ДополнительныеПараметры);

КонецПроцедуры

&НаСервере
Процедура ПослеЗагрузкиИзФайлаСервер(Результат, ДополнительныеПараметры)
	
	Если Не Результат = Неопределено Тогда
		ТаблицаДанных=ПолучитьИзВременногоХранилища(Результат);
		
		Если ТаблицаДанных<>Неопределено Тогда
			Объект.УточнениеПоНоменклатуре.Загрузить(ТаблицаДанных);
		КонецЕсли;
		для каждого стр из Объект.УточнениеПоНоменклатуре цикл
			стр.БазовыйВидЦены= Объект.БазовыйВидЦены;
		конеццикла;	
	КонецЕсли;

КонецПроцедуры

#КонецОбласти

&После ("НастроитьВидимостьЭлементовФормы")
&НаКлиентеНаСервереБезКонтекста
Процедура Док_НастроитьВидимостьЭлементовФормы(Форма)
	Форма.Элементы.СтраницаУточнениеПоНоменклатуре.Видимость=Форма.Элементы.СтраницаУточнениеПоЦеновымГруппам.Видимость;
	//Форма.Элементы.УточнениеПоНоменклатуреБазовыйВидЦены.Видимость=Форма.Элементы.ФормулыБазовыйВидЦены.Видимость;
	//Форма.Элементы.УточнениеПоНоменклатуреФормула.Видимость=Форма.Элементы.ФормулыФормула.Видимость;
КонецПроцедуры

&НаКлиенте
Процедура Док_ГруппаВидовЦенПриИзмененииПосле(Элемент)
	 СоставитьНаименование(); 
КонецПроцедуры

&НаКлиенте
Процедура СоставитьНаименование()

	если значениезаполнено(Объект.ГруппаВидовЦен) и значениезаполнено(Объект.РегионПрисутствия)
		и значениезаполнено(Объект.ФорматПодразделения) тогда
		 Объект.Наименование=""+Объект.ГруппаВидовЦен+"_"+Объект.РегионПрисутствия+"_"+Объект.ФорматПодразделения;
	конецесли;	

КонецПроцедуры

&НаКлиенте
Процедура Док_РегионПрисутствияПриИзмененииПосле(Элемент)
	 СоставитьНаименование();
КонецПроцедуры

&НаКлиенте
Процедура Док_ФорматПодразделенияПриИзмененииПосле(Элемент)
 СоставитьНаименование();	
КонецПроцедуры
