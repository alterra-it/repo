#Область ОбработчикиСобытийФормы_Справочники
&НаСервере
Процедура Док_ПриСозданииНаСервереПосле(Отказ, СтандартнаяОбработка)
	Элементы.Форма_СоздатьОграниченноГодный1.Видимость=рольДоступна("ОтчетыИОбработкиКладовщика") И Не рольДоступна("ДобавлениеИзменениеНоменклатуры");
КонецПроцедуры

#КонецОбласти

#Область КомандыФормы_Справочники

&НаКлиенте
Процедура Док__СоздатьОграниченноГодныйВместо(Команда)
	Док__СоздатьОграниченноГодныйВместоНаСервере();
	Элементы.Список.Обновить();
КонецПроцедуры

#КонецОбласти

#Область Вспомогательные_процедуры_и_функции

&НаСервере
Процедура Док__СоздатьОграниченноГодныйВместоНаСервере()
	
	Если рольДоступна("ОтчетыИОбработкиКладовщика") И Не рольДоступна("ДобавлениеИзменениеНоменклатуры") Тогда
		УстановитьПривилегированныйРежим(Истина);
		СоздатьОграниченноГодный();
		УстановитьПривилегированныйРежим(Ложь);
	Иначе
		СоздатьОграниченноГодный();
	КонецЕсли;

КонецПроцедуры

&НаСервере
Процедура СоздатьОграниченноГодный() 
	Справочники.Номенклатура.СоздатьОграниченноГодныйТовар(Номенклатура);
КонецПроцедуры

#КонецОбласти