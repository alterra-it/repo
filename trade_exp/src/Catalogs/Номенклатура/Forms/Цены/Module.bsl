////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ ФОРМЫ

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	// Пропускаем инициализацию, чтобы гарантировать получение формы при передаче параметра "АвтоТест".
	Если Параметры.Свойство("АвтоТест") Тогда
		Возврат;
	КонецЕсли;
	
	АвтоЗаголовок = Ложь;
	Заголовок = НСтр("ru = 'Цены'");
	
	ОтборНоменклатура = Параметры.Ключ;
	
	ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбора(
		ЦеныНоменклатуры.Отбор, 
		"Номенклатура", 
		ОтборНоменклатура, 
		ВидСравненияКомпоновкиДанных.Равно, 
		"Номенклатура", 
		Истина);
	
	Если РольДоступна("ПолныеПрава") Или РольДоступна("ЧтениеЦенПартнеров") 
		Или РольДоступна("ДобавлениеИзменениеАссортиментаИЦенПартнеров") Тогда
		
		ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбора(
			ЦеныНоменклатурыПоставщиков.Отбор, 
			"Номенклатура", 
			ОтборНоменклатура, 
			ВидСравненияКомпоновкиДанных.Равно, 
			"Номенклатура", 
			Истина);
		
		Элементы.Контрагент.Видимость = ПолучитьФункциональнуюОпцию("ИспользоватьПартнеровИКонтрагентов");
		
	КонецЕсли;

	СписокВидовЦен = Новый СписокЗначений();
	СписокВидовЦен.ЗагрузитьЗначения(Справочники.ВидыЦен.ДоступныеВидыЦен());

	ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбора(
		ЦеныНоменклатуры.Отбор, 
		"ВидЦены", 
		СписокВидовЦен, 
		ВидСравненияКомпоновкиДанных.ВСписке, 
		"ВидЦеныОграничениеПоПравам", 
		Истина);
	
	Элементы.Контрагент.Видимость = ПолучитьФункциональнуюОпцию("ИспользоватьПартнеровИКонтрагентов");
	
	УстановитьСтандартныеНастройки("ПрайсЛист");
	СформироватьОтчет("ПрайсЛист");
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаВыбора(ВыбранноеЗначение, ИсточникВыбора)
	
	Результат = Ложь;
	
	Если ИсточникВыбора.КлючТекущегоВарианта = "ПоНоменклатуреКонтекст" Тогда
		ИмяОтчета = "ПрайсЛист";
	КонецЕсли;
	
	ЭтаФорма[ИмяОтчета].КомпоновщикНастроек.ЗагрузитьПользовательскиеНастройки(ВыбранноеЗначение.ПользовательскиеНастройки);
	СформироватьОтчет(ИмяОтчета);
	
КонецПроцедуры

////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ ЭЛЕМЕНТОВ ШАПКИ ФОРМЫ

&НаКлиенте
Процедура ПериодЦеныНоменклатурыПриИзменении(Элемент)	
	
	ПериодЦеныНоменклатурыПриИзмененииНаСервере();
	
КонецПроцедуры

&НаКлиенте
Процедура ВидЦеныНоменклатурыПриИзменении(Элемент)
	
	ВидЦеныНоменклатурыПриИзмененииНаСервере();
	
КонецПроцедуры

&НаКлиенте
Процедура ПериодЦеныНоменклатурыПоставщикаПриИзменении(Элемент)
	
	ПериодЦеныНоменклатурыПоставщиковПриИзмененииНаСервере();
	
КонецПроцедуры

&НаКлиенте
Процедура ПериодЦеныНоменклатурыПоставщикаОчистка(Элемент, СтандартнаяОбработка)
	
	ПериодЦеныНоменклатурыПоставщиковПриИзмененииНаСервере();
	
КонецПроцедуры
 
&НаКлиенте
Процедура ПартнерПриИзменении(Элемент)
	
	ПартнерПриИзмененииНаСервере();
	
КонецПроцедуры

&НаКлиенте
Процедура СоглашениеПриИзменении(Элемент)
	
	СоглашениеПриИзмененииНаСервере();
	
КонецПроцедуры

&НаКлиенте
Процедура УсловиеЦеныПриИзменении(Элемент)
	
	 УсловиеЦеныПриИзмененииНаСервере();
	
КонецПроцедуры

&НаКлиенте
Процедура ТаблицаПрайсЛистОбработкаРасшифровки(Элемент, Расшифровка, СтандартнаяОбработка)
	
	ОписаниеОбработкиРасшифровки = КомпоновкаДанныхКлиент.ОбработатьРасшифровку(ЭтаФорма, "ПрайсЛист", Расшифровка, СтандартнаяОбработка); 
	
	Если ОписаниеОбработкиРасшифровки <> Неопределено Тогда
		ОбработатьРасшифровкуОтчета("ПрайсЛист", ОписаниеОбработкиРасшифровки);
	КонецЕсли;

КонецПроцедуры

////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ КОМАНД ФОРМЫ

&НаКлиенте
Процедура СформироватьОтчетПрайсЛист(Команда)
	
	СформироватьОтчет("ПрайсЛист");
	
КонецПроцедуры

&НаКлиенте
Процедура НастроитьОтчетПрайсЛист(Команда)
	
	НастроитьОтчет("ПрайсЛист");
	
КонецПроцедуры

////////////////////////////////////////////////////////////////////////////////
// СЛУЖЕБНЫЕ ПРОЦЕДУРЫ И ФУНКЦИИ

////////////////////////////////////////////////////////////////////////////////
// Прочее

&НаСервере
Процедура СформироватьОтчет(ИмяРеквизитаФормы)
	
	ИмяОтчетаВарианта = ИмяОтчетаИВариантаПоИмениРеквизитаФормы(ИмяРеквизитаФормы);
	КомпоновкаДанныхСервер.СформироватьОтчет(ЭтаФорма, ИмяРеквизитаФормы, ИмяОтчетаВарианта.ИмяОтчета, ИмяОтчетаВарианта.ИмяВариантаОтчета); 
	
КонецПроцедуры

&НаКлиенте 
Процедура НастроитьОтчет(ИмяРеквизитаФормы)
	
	ИмяОтчетаВарианта = ИмяОтчетаИВариантаПоИмениРеквизитаФормы(ИмяРеквизитаФормы);
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("Вариант",                   ЭтаФорма[ИмяРеквизитаФормы].КомпоновщикНастроек.Настройки);
	ПараметрыФормы.Вставить("КлючВарианта",              Строка(ИмяОтчетаВарианта.ИмяВариантаОтчета));
	ПараметрыФормы.Вставить("ПользовательскиеНастройки", ЭтаФорма[ИмяРеквизитаФормы].КомпоновщикНастроек.ПользовательскиеНастройки);
	ПараметрыФормы.Вставить("ПредставлениеВарианта",     ИмяОтчетаВарианта.ПредставлениеОтчета);
	
	ПараметрыФормы.Вставить("ПредставлениеПользовательскихНастроек", "");
	ПараметрыФормы.Вставить("РежимВыбора", Истина);
	
	ОткрытьФорму("Отчет." + ИмяОтчетаВарианта.ИмяОтчета + ".ФормаНастроек", ПараметрыФормы, ЭтаФорма);
	
КонецПроцедуры

&НаСервере
Функция ИмяОтчетаИВариантаПоИмениРеквизитаФормы(ИмяРеквизитаФормы)
	
	Результат = Новый Структура("ИмяОтчета, ПредставлениеОтчета, ИмяВариантаОтчета");
	
	Если ИмяРеквизитаФормы = "ПрайсЛист" Тогда
		
		Результат.ИмяОтчета           = "ПрайсЛист";
		Результат.ПредставлениеОтчета = НСтр("ru= 'Прайс-лист'") ;
		если ДопМодуль.НовоеЦенообразование() тогда
			Результат.ИмяВариантаОтчета   = "ПоНоменклатуреКонтекстНовоеЦО";
		иначе	
			Результат.ИмяВариантаОтчета   = "ПоНоменклатуреКонтекст";
		конецесли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервере
Процедура ОбработатьРасшифровкуОтчета(ИмяРеквизитаФормы,ОписаниеОбработкиРасшифровки)
	
	КомпоновкаДанныхСервер.ОбработатьРасшифровку(ЭтаФорма, ИмяРеквизитаФормы, ОписаниеОбработкиРасшифровки);
	
КонецПроцедуры

&НаСервере
Процедура УстановитьСтандартныеНастройки(ИмяРеквизитаФормы)
	
	ИмяОтчетаВарианта = ИмяОтчетаИВариантаПоИмениРеквизитаФормы(ИмяРеквизитаФормы);
	
	Отбор = Новый Структура;
	Отбор.Вставить("Номенклатура", ОтборНоменклатура);
	
	КомпоновкаДанныхСервер.УстановитьСтандартныеНастройки(ЭтаФорма, ИмяРеквизитаФормы, ИмяОтчетаВарианта.ИмяОтчета, 
		ИмяОтчетаВарианта.ИмяВариантаОтчета, Отбор);
	
КонецПроцедуры

&НаСервере
Процедура ПериодЦеныНоменклатурыПриИзмененииНаСервере()
	
	ОбщегоНазначенияКлиентСервер.УдалитьЭлементыГруппыОтбора(ЦеныНоменклатуры.Отбор,"Период","Период");
	
	ГруппаЭлементовОтбора = ОбщегоНазначенияКлиентСервер.СоздатьГруппуЭлементовОтбора(
		ЦеныНоменклатуры.Отбор.Элементы,
		"ЦеныНоменклатурыОтборПоПериоду",
		ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИ);
	
	ОбщегоНазначенияКлиентСервер.ДобавитьЭлементКомпоновки(
		ГруппаЭлементовОтбора,
		"Период",
		ВидСравненияКомпоновкиДанных.БольшеИлиРавно,
		ПериодЦеныНоменклатуры.ДатаНачала,
		"Период",
		ЗначениеЗаполнено(ПериодЦеныНоменклатуры.ДатаНачала));
	
	// Установить элемент отбора в группе.
	ОбщегоНазначенияКлиентСервер.ДобавитьЭлементКомпоновки(ГруппаЭлементовОтбора, "Период", ВидСравненияКомпоновкиДанных.МеньшеИлиРавно, 
		ПериодЦеныНоменклатуры.ДатаОкончания, "Период", ЗначениеЗаполнено(ПериодЦеныНоменклатуры.ДатаОкончания));
	
КонецПроцедуры

&НаСервере
Процедура ВидЦеныНоменклатурыПриИзмененииНаСервере()
	
	ОбщегоНазначенияКлиентСервер.УдалитьЭлементыГруппыОтбора(ЦеныНоменклатуры.Отбор,,"ВидЦены");
	
	ОбщегоНазначенияКлиентСервер.ДобавитьЭлементКомпоновки(ЦеныНоменклатуры.Отбор, "ВидЦены", 
		ВидСравненияКомпоновкиДанных.Равно, ВидЦеныНоменклатуры, "ВидЦены", ЗначениеЗаполнено(ВидЦеныНоменклатуры));
	
КонецПроцедуры

&НаСервере
Процедура ПериодЦеныНоменклатурыПоставщиковПриИзмененииНаСервере()
	
	ОбщегоНазначенияКлиентСервер.УдалитьЭлементыГруппыОтбора(ЦеныНоменклатурыПоставщиков.Отбор,"Период","Период");
	
	ГруппаЭлементовОтбора = ОбщегоНазначенияКлиентСервер.СоздатьГруппуЭлементовОтбора(ЦеныНоменклатурыПоставщиков.Отбор.Элементы,
		"ЦеныНоменклатурыОтборПоПериоду", ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИ);
	
	ОбщегоНазначенияКлиентСервер.ДобавитьЭлементКомпоновки(ГруппаЭлементовОтбора, "Период", ВидСравненияКомпоновкиДанных.БольшеИлиРавно,
		ПериодЦеныНоменклатурыПоставщика.ДатаНачала, "Период", ЗначениеЗаполнено(ПериодЦеныНоменклатурыПоставщика.ДатаНачала));
	
	// Установить элемент отбора в группе.
	ОбщегоНазначенияКлиентСервер.ДобавитьЭлементКомпоновки(ГруппаЭлементовОтбора, "Период", 
		ВидСравненияКомпоновкиДанных.МеньшеИлиРавно, ПериодЦеныНоменклатурыПоставщика.ДатаОкончания, "Период", 
		ЗначениеЗаполнено(ПериодЦеныНоменклатурыПоставщика.ДатаОкончания));
	
КонецПроцедуры

&НаСервере
Процедура ПартнерПриИзмененииНаСервере()
	
	ОбщегоНазначенияКлиентСервер.УдалитьЭлементыГруппыОтбора(ЦеныНоменклатурыПоставщиков.Отбор,"Контрагент","Контрагент");
	
	ОбщегоНазначенияКлиентСервер.ДобавитьЭлементКомпоновки(ЦеныНоменклатурыПоставщиков.Отбор, "Контрагент",
		ВидСравненияКомпоновкиДанных.Равно, Контрагент, "Контрагент", ЗначениеЗаполнено(Контрагент));
	
КонецПроцедуры

&НаСервере
Процедура СоглашениеПриИзмененииНаСервере()
	
	ОбщегоНазначенияКлиентСервер.УдалитьЭлементыГруппыОтбора(ЦеныНоменклатурыПоставщиков.Отбор,"Соглашение","Соглашение");
	
	ОбщегоНазначенияКлиентСервер.ДобавитьЭлементКомпоновки(ЦеныНоменклатурыПоставщиков.Отбор,
		"Соглашение", ВидСравненияКомпоновкиДанных.Равно, Соглашение, "Соглашение", ЗначениеЗаполнено(Соглашение));
	
КонецПроцедуры

&НаСервере
Процедура УсловиеЦеныПриИзмененииНаСервере()
	
	ОбщегоНазначенияКлиентСервер.УдалитьЭлементыГруппыОтбора(ЦеныНоменклатурыПоставщиков.Отбор,"УсловиеЦеныПоставщика","УсловиеЦеныПоставщика");
	
	ОбщегоНазначенияКлиентСервер.ДобавитьЭлементКомпоновки(ЦеныНоменклатурыПоставщиков.Отбор, "УсловиеЦеныПоставщика",
		ВидСравненияКомпоновкиДанных.Равно, УсловиеЦеныПоставщика, "УсловиеЦеныПоставщика", ЗначениеЗаполнено(УсловиеЦеныПоставщика));
	
КонецПроцедуры