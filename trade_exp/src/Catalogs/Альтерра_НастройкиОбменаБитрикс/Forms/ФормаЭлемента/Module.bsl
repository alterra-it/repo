
&НаКлиенте
Процедура НаименованиеПриИзменении(Элемент)
	Объект.Код = СтрЗаменить(Объект.Наименование, " ","_");
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	ОбновитьСписокВыбора();
	ИзменитьФильтр("");
КонецПроцедуры

&НаКлиенте
Процедура РедактироватьСоставСписочныхРеквизитов(Команда)
	Элементы.ГруппаРеквизитыТипаСписок.Видимость = Истина;
КонецПроцедуры

&НаКлиенте
Процедура СохранитьСписок(Команда)
	Записать();
	Элементы.ГруппаРеквизитыТипаСписок.Видимость = Ложь;
	ОбновитьСписокВыбора();
КонецПроцедуры

&НаСервере
Процедура ОбновитьСписокВыбора()
		РеквизитыТипаСписок = Объект.РеквизитыТипаСписок;
		ПолеВыбора = Элементы.ПолеБитрикс;
		ПолеВыбора.СписокВыбора.Очистить();
		Для номСтр=0 по РеквизитыТипаСписок.Количество()-1 Цикл 
			ПолеВыбора.СписокВыбора.Добавить(РеквизитыТипаСписок[номСтр].ИдПоля, РеквизитыТипаСписок[номСтр].НаименованиеПоляБитрикс);
		КонецЦикла;
		Если ПолеВыбора.СписокВыбора.Количество() > 0 Тогда
			ИзменитьФильтр(ПолеВыбора.СписокВыбора[0]);
		КонецЕсли;
КонецПроцедуры // ОбновитьСписокВыбора()


&НаКлиенте
Процедура ПолеБитриксОбработкаВыбора(Элемент, ВыбранноеЗначение, СтандартнаяОбработка)
	ИзменитьФильтр(ВыбранноеЗначение);
КонецПроцедуры

&НаСервере
Процедура ИзменитьФильтр(ЗначениеФильтрации)
	Отбор = Новый ФиксированнаяСтруктура(Новый Структура("ИдПоляСпискаВБитриксе", ЗначениеФильтрации));
	Элементы.ЗначенияСписковДляОбъекта.ОтборСтрок = Отбор;
КонецПроцедуры // ИзменитьФильтр()

&НаКлиенте
Процедура ЗначенияСписковДляОбъектаПередОкончаниемРедактирования(Элемент, НоваяСтрока, ОтменаРедактирования, Отказ)
	Элемент.ТекущиеДанные.ИдПоляСпискаВБитриксе = ПолеБитрикс;
	Модифицированность = Истина;
КонецПроцедуры

