
&НаСервере
Процедура Док_ПриСозданииНаСервереПеред(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("МассивСсылокИерархия") И ЗначениеЗаполнено(Параметры.МассивСсылокИерархия) Тогда
		ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(
			Список,
			"Ссылка.Родитель",
			Параметры.МассивСсылокИерархия,
			ВидСравненияКомпоновкиДанных.ВСписке,
			,
			Истина); 
	КонецЕсли;
		
	Если Параметры.Свойство("РабочийУчасток") И ЗначениеЗаполнено(Параметры.РабочийУчасток) Тогда
		ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(
			Список,
			"РабочийУчасток",
			Параметры.РабочийУчасток,
			ВидСравненияКомпоновкиДанных.Равно,
			,
			Истина);  
	КонецЕсли;

КонецПроцедуры
