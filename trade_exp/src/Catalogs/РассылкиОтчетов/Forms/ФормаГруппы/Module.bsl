
&НаСервере
Процедура Док_ПриСозданииНаСервереПосле(Отказ, СтандартнаяОбработка)
	
	НовыйЭлементКонтроль = ЭтотОбъект.Элементы.Добавить("КонтрольРассылок", Тип("ПолеФормы"), Элементы.КонтрольРассылокГруппа);
	НовыйЭлементКонтроль.Вид = ВидПоляФормы.ПолеВвода;
	НовыйЭлементКонтроль.ПутьКДанным = "Объект.КонтрольРассылок";
	
	НовыйЭлементИДЧата = ЭтотОбъект.Элементы.Добавить("Идчата", Тип("ПолеФормы"), Элементы.КонтрольРассылокГруппа);
	НовыйЭлементИДЧата.Вид = ВидПоляФормы.ПолеВвода;
	НовыйЭлементИДЧата.ПутьКДанным = "Объект.ИДЧата";
	
	Если АльтерраОбщегоНазначения._РольДоступна("ПолныеПрава") ИЛИ АльтерраОбщегоНазначения._РольДоступна("ОтделАналитики") Тогда
		НовыйЭлементКонтроль.Видимость = Истина;
		НовыйЭлементИДЧата.Видимость = Истина;
	Иначе
		НовыйЭлементКонтроль.Видимость = Ложь;
		НовыйЭлементИДЧата.Видимость = Ложь;
	КонецЕсли;
	
КонецПроцедуры
